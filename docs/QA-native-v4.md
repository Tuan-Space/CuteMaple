# v4 修复与验收记录

状态（2026-09-09 11:32）：v4 原生导出、动作审查、目录构建与 20 次启动及一小时运行已完成。真实键盘、显示工作区变化和 Editor 内 CAN3 播放仍待确认；目录包保留候选标记，尚未执行最终发布标记。以下历史阶段结论以各自时间和素材版本为准。

## 本轮交付状态

实际运行包为 `dist/CuteMaple-Live2D-Directory-20260909-100835/CuteMaple-Live2D/CuteMaple-Live2D.exe`，SHA256 `d789cc4539adc6351349de24cc30c3d7f5596a0271cc832934113775e707ab8a`。本次构建完整 Python 测试 **303 项通过**，打包程序、原生渲染与独立清理助手的非特权诊断均通过。后续补测和发布元数据工具的 **78 项针对性测试**另行通过，最后对 PowerShell 等价时区序列化的限定处理又完成 **30 项聚焦回归**；这些是不同阶段的检查，不相加为独立测试数，也没有重新编译或修改已测程序及模型。[真实原件重验](../artifacts/candidate-v4-20260909-100835/actual-base-timezone-pending-recheck.json)读取 212 个原件且前后无变化，仍准确拒绝缺失的真实键盘检查，没有生成通过标记。

实际默认 Windows 图形后端、普通 PetWindow 和原生活动提供器下，**20 次启动各运行至少 20 秒，随后同一程序连续运行 3600.0017435 秒**。21 次程序均退出码 0，无遗留自有子进程；默认四项 Windows 防护始终开启。完整观察区间为本地时间 **10:17:12–11:25:59**，无匹配候选的防护检测；退出后额外查询 Application 1000/1001/1002/1026 也无匹配记录。[原始桌面报告](../artifacts/desktop-v4-final-20260909-1017/NORMAL-DESKTOP-QA.json)、[独立原件复核](../artifacts/candidate-v4-20260909-100835/completed-desktop-readonly-review.json)。原始 aggregate 的 `passed=false/PENDING` 保留，不改成最终通过。

鼠标移动与原生视线、真实系统声音回应、暂停恢复、真实拖放均有记录。两次拖放分别验证拖至顶部接入秋千，以及释放后下落、落地。键盘事件与显示变化事件仍为 0，不能判通过；锁屏、系统睡眠恢复、音频设备更换及手动系统集成也没有本轮完整实测。可在同一 EXE 和全部 53 个模型/前端资源不变的条件下补做真实交互，保留本次独立一小时及 20 次启动，不相加时长，详见 [补测流程](QA-desktop.md)。

21 次技术运行均留下一条初始化 `0x8001010d` 记录，随后载入原生模型并正常退出。独立复核绑定全部故障日志哈希、后续活动、退出和 WER 区间，仅认可“本轮已恢复”，不宣称触发原因已完全定位或记录已消除。防护结论也仅覆盖已观察的时间段。

11:28 已把同一候选以普通用户、无测试参数或图形覆盖项在日常配置中重新打开。实际旧配置备份与预先保存的原始字节哈希一致；已迁移到 schema 3，视线跟随开启，开机启动和自动清理关闭，Live2D ready 且进程响应正常。额外启动区间未发现匹配的 Windows 事件。[日常启动记录](../artifacts/candidate-v4-20260909-100835/normal-profile-check.json)。这是仍在运行的观察，不冒充一次完成的退出测试。

可编辑 CAN3 与源 CMO3、实际导出及 23 段动作的结构和来源核对一致：56 参数、1288 轨道、3393 关键点；100 fps 时间轴转换后的逐值差为 0，中间键的时间量化最多 5 ms。`editorPlaybackVerified=false` 明确保留，不能以 CMO3 导出成功替代 CAN3 实际播放。[作者动画核对](../artifacts/native-v4-final-20260909/can3-source-alignment.json)。

一小时性能采样共 721 条：16 逻辑核整机 CPU 均值约 4.46%，RSS 合计中位约 751 MiB、峰值约 1023 MiB。末段三个自有进程工作集同时下降，身份与动画活动保持，原因未由此确定；不能用较低尾值代表常驻占用。采样包含每秒 QA 画面读取，RSS 也会重复计算共享页，[性能分析](../artifacts/desktop-v4-final-20260909-1017/performance/analysis.json)不等同于纯待机开销。

## 已核实的问题

- v3 的躯干与袖子残留手指、袖口，抬臂后出现多余的手；攀爬没有真正近侧面的头身关键形，登顶直接切到秋千。
- 最终构建 `20260908-190759` 的单文件版在 2026-09-08 19:20 后出现 Defender Persistence/Bearfoos 检测与隔离；目录版于 19:51 出现 Bearfoos 检测。不得再推荐、恢复隔离或反复启动这两个产物。
- 普通启动会自动写开机启动项，单文件模式把临时展开的 `sys.executable` 注册为持久启动路径。此为确定的代码缺陷，但日志不足以证明其解释了全部检测或断言误报。
- 音频工作线程退出有无限等待路径；渲染失败后仍可能接受迟到的 ready；显示器对象未处理移除与远程会话替换。这些是明确故障面，尚未证明为本次被终止的唯一原因。
- 历史包测试采用 offscreen，关闭了开机启动、清理授权和全局活动感知；普通应用运行约 1.6 秒。另一个模型渲染检查不经过完整 PetWindow。历史测试不能证明普通桌面长时间稳定。

## 本次验收门槛

- 全部 21 个功能动作与登顶过渡的原生连续录像及中间帧检查。
- 真正左右近侧面、每侧一只手、衣袖无空洞穿插、稳定支撑及固定秋千悬点。
- 默认图形后端、可见窗口与活动感知开启的 60 分钟运行；20 次启动与退出；会话、显示器、音频设备变化及隐藏恢复。
- 启动路径、旧设置迁移、手动启用系统集成、音频进程超时、渲染加载终态和动作中断的回归测试。
- 原生 Editor 导出的 MOC3 与 v4 CMO3/CAN3/贴图匹配；目录版资源清单与 SHA-256 记录。
- 默认 Windows 防护保持开启。若候选产物被检测，停止发布并保留记录，不以排除项或隔离恢复作为通过条件。

## 结果

### 已完成的程序修复

- 默认关闭开机启动和清理授权；旧配置先备份再迁移，普通启动不创建系统任务或写开机启动项。清理实现拆到不加载 Qt 的独立程序。
- 2026-09-08 20:41，在备份并逐字核对后，一次性清除了本机 HKCU Run 的“美腻枫”失效值：它仍指向旧 `onefile_15744_134333400238252025` 临时目录内的 `python.exe`。删除后确认该值不存在，未修改其他启动项或安全设置。备份与结果位于 [system-integration](../artifacts/system-integration/)。未发现旧的“美腻枫_内存清理”计划任务。
- 系统音量采样由普通权限子进程承担，关闭时限明确；渲染加载增加批次与失败终态，忽略旧回调；处理屏幕移除后的重新定位。
- 攀爬至顶部增加左右两段内部动作及原生接触事件，保留原有 21 类公开动作。
- 附着期间的双击、抚摸、说话和活动回应保留支撑姿势，避免切换成地面动作；限时表情与活动表情合并处理，防止彼此误清除。相关应用与互动测试 46 项通过。
- 最终作者候选的完整 Python 检查 202 项通过（83.46 秒，零失败、零跳过）；[测试摘要](../artifacts/python-tests-authoring-freeze-20260908-225350.summary.json)绑定冻结 IR 的哈希。Web 测试 15 项、TypeScript 检查和构建已通过，前端资源清单匹配。真实 v4 原生导出仍待完成。

### 已完成的有限运行检查

以下检查均使用 **v3 模型与修改后的源码**，不是 v4 动作验收或最终运行包验收。

- 默认图形后端的可见桌面窗口运行约 21 秒，RawInput 与原生音频采样子进程成功启动，正常退出后未遗留音频子进程。报告：[desktop-hardening-v3/report.json](../artifacts/desktop-hardening-v3/report.json)。
- 人为触发 WebGL 失效后，新一轮加载恢复可见模型；报告：[renderer-recovery-v3.json](../artifacts/renderer-recovery-v3.json)。
- 新目录候选的独立清理程序通过无权限诊断：正常退出、不加载 Qt、不执行清理或授权操作，观察期间无匹配 Defender 事件。
- 新目录候选的主程序在桌面检查入口暴露 Nuitka 路径差异：`containing_dir` 指向包目录的上一层。已改为优先使用实际绝对启动路径并通过回归测试；该次候选未进入 Qt 桌面，不能算运行通过，待重新编译复验。
- 路径修正后的 `20260908-205739` 目录候选已实际运行 20.0096 秒，使用 Windows 默认平台与图形后端，Live2D、RawInput、音频子进程活动正常；退出码 0，无遗留自有子进程。观察区间 21:01:44–21:02:16，四项默认防护状态开启且前后未变，无匹配该候选的 Defender 检测。[实际目录包短检报告](../artifacts/candidate-v3-205739-desktop/NORMAL-DESKTOP-QA.json)。这仍是 v3 素材的程序候选，且最终编译还需纳入之后修正的拖放观察器。
- 静止鼠标位置与计算出的视线目标匹配；尚未实际验证连续鼠标移动、键盘输入、音频设备切换、显示器切换及锁屏恢复。
- 增加只读原生 Core 计算基准工具 `tools/authoring/benchmark_moc3.mjs`，按作者动作曲线采样并检查所有顶点为有限数值。v3 的 21 个动作共测 1890 次 update，6301 顶点，中位 0.272 ms、p95 0.363 ms；[基准记录](../artifacts/core-benchmark-v3-20260908.json)。它不包含 Qt、GPU、物理或硬件交互，不能替代桌面测试；v4 须用实际导出重测。

### 尚未消除的初始化异常记录

源码和 `205739` 候选均在创建 `QWebEngineProfile` 附近留下 `0x8001010d` 原生异常记录，随后继续载入模型并正常退出；该区间未发现匹配的 Application/WER/Hang 事件。按[微软错误码说明](https://learn.microsoft.com/en-us/windows/win32/com/com-error-codes-3)，此码表示同步输入调用期间的 COM 外呼限制。[CPython 3.13.7 实现](https://raw.githubusercontent.com/python/cpython/v3.13.7/Modules/faulthandler.c)会在异常继续交给后续处理前写出记录，因此日志中的 “Windows fatal exception” 字样不能单独证明进程已经终止。本次恢复有运行记录支持；具体触发上下文尚未完全定位，不宣称没有异常或已经根治。

四次只创建 QApplication 与 QWebEngineProfile 的独立探针（事件循环前、循环后各两次）均未复现该码，不能把它解释为所有 Profile 构造都会发生的现象；详见 [隔离探针报告](../artifacts/webengine-profile-probe-20260908/summary.json)。

随后保留普通应用、默认提供器与图形后端，仅在独立测试入口比较原始调度与延迟 250 毫秒初始化：两次各运行 10 秒，均继续就绪、正常退出，也都记录同一码；延迟版本记录于 `app.exec()` 上下文。此实验没有证明延迟可以修复，未据此修改普通应用。记录见 [调度对比报告](../artifacts/runtime-init-delay-probe-20260908/summary.json)。

### 待完成

v4 素材与关节绑定精修、原生 Editor 导出、连续动作审图、最终目录包的默认防护检查、60 分钟运行和 20 次重启。当前构建候选仍含 v3 模型，不能作为 v4 成品交付。

### 作者工程连续帧预检

`artifacts/ir-v4-left-final` 保存了第一次候选的 37 张连续离线 IR 帧、曲线采样、输入哈希和 GIF。输入在该次渲染中未变化。这不是 Cubism 原生播放结果，也未通过视觉验收：0.8–1.2 秒处暴露手臂叠加位移、头身视角切换时颈部提前隐藏，以及站姿和坐姿裙摆长时间重叠。已据此退回修改，不导出该候选作为成品。

22:53 的作者候选已修正上述问题：单一攀爬至握绳的关节路径、进入活动姿态时提前退休旧手、分别连接头身的颈部网格，以及对齐后的短站坐材质交换。134 部件、56 参数、23 场景。29 项作者/CAN3/视角检查、8 项实际分层图像连接回归及 10 项图集/有界 CAFF 检查通过；随后完整集成测试 202 项通过。

左右各 37 帧的新预检在 `artifacts/ir-v4-left-path` 与 `artifacts/ir-v4-right-path`，输入在两次渲染中均未变化。关键帧已不再出现旧手残留、叠加臂位移和整条断颈；视角和站坐材质短交换仍需在实际原生播放中检查。它们是离线 IR 预检，不能作为原生或桌面运行证据。

首份待导出的工程打包到 `F:\cutemaple\Maple-v4-Export`，使用单张 4096 图集。源 CMO3 的 SHA-256 为 `d1276c0c072c79ee6ee9a6f3c3f98884d78d1eddcce297d246d165032fe35634`。当时的静态检查覆盖裁剪像素与 UV，但没有覆盖 Editor 对嵌套类声明和纹理资源状态的读取；下面记录首次实际导出揭示的问题。

打包 CMO3 的 SHA-256 为 `59ed345672046aa2de09a92b9f0dcebf425a06f52346f0613ff2cb128e83094b`，重定位后的 CAN3 为 `e552aa3a504e812bec5330099e657d4543bf5d543ba506b18141c5bce52038f6`。电脑工具恢复窗口后仍无法打开 Cubism 的“文件”菜单；已把模型、动画、图集和中文导出说明准备在同目录，请用户代做原生导出。未恢复隔离文件、增加排除项或改变防护设置。

### 2026-09-09 首次 v4 原生导出诊断

00:27:38 的实际新导出保存在旧目录 `F:\cutemaple\Maple-Export`。MOC3 为 28,623,104 字节，SHA-256 `4bec1d95e85e716c09df221061fc230764685c36bb5d019dbf52119c31180f79`；官方 Core 一致性检查通过，包含 56 参数、134 网格。该目录的 model3.json 仍引用旧 2048 图集，而新 4096 PNG 的 alpha 全为 0，不能直接作为可用模型安装。

Editor 日志明确记录 268 条 ModelImageEntry 类或序列化器缺失错误。打包器漏掉嵌套类导入，并保留了不匹配的 MODEL_IMAGE 纹理状态和源图资源链接。已修正为图集资源状态并补齐声明，新增真实打包结构回归；修正后的工程仍须再次通过 Editor 打开与导出确认。原始模型和用户导出均保留。

在 `.build/native-v4-texture-recovery` 中，用未修改的实际 MOC3 和作者原始图集建立了独立诊断副本，并明确记录图集与 model3.json 是重新配对的诊断资源。130/134 个网格 UV 与作者图集匹配；4 个非正面颈部网格的 UV 被重新投影，最大误差约 90.23 图集像素，不能作为最终模型。原始导出与来源记录在 [native-export-20260909-002738](../artifacts/native-export-20260909-002738/)。正式 `assets/live2d/Maple` 仍是 v3。

该隔离副本实际完成 23 动作、43 次动作/功能截图；验收失败，保留完整失败结果：[原生报告](../artifacts/native-v4-diagnostic-known-neck-uv/report.json)。独立新建 Core.Model 逐参数复核也重现了转身透明度、视角绘制顺序和站坐材质重叠问题，不能归为测试误报或忽略。连续左攀爬到登顶的 223 张真实帧用于定位这些问题；[连续帧记录](../artifacts/native-v4-diagnostic-known-neck-uv/climb-left-continuous.json)中的 passed 仅表示录制完整、资源未变化，不表示视觉验收通过。

首次实际 v4 的官方 Core CPU 基准共 2070 次 update、30,822 顶点，中位 2.338 ms、p95 2.814 ms，所有顶点为有限数值。[诊断基准](../artifacts/core-benchmark-v4-first-native-20260909.json)。这份导出仍有上述外观缺陷，数据不能替代修正后的模型或完整桌面性能验收。

隔离安装同时暴露了 Python 3.13 在 Windows 上通过 mkdtemp 创建私有 ACL 的跨进程读取问题：sandbox 内安装的目录对普通桌面进程不可读，报 WinError 5。安装器已改用同父目录的随机 UUID 名称与原子 mkdir，继承父目录权限，并拒绝透明度全零的 PNG 图集。回滚增加备份所有权标志，避免极端名称冲突时误用已有备份。没有修改已有目录 ACL 或 Windows 防护。

修正后的安装器已在 sandbox 中向新的隔离目标完成安装，普通 Windows 用户随后实际读取全部 29 个文件，共 33,382,884 字节；路径、长度和 SHA-256 与 sandbox 记录完全一致。[跨账户读取验证](../artifacts/installer-inherited-acl-summary.json)。这是已知无效颈部 UV 副本的安装权限验证，不是模型视觉验收。

安装修复后的完整 Python 测试 215 项通过，零失败、零跳过，11 个相关源码哈希在测试期间未变；[测试摘要](../artifacts/python-tests-native-export-repair-20260909-005045.summary.json)。此后原生诊断新增真实 renderOrder 字段（而非把作者 drawOrder 当成实际排序），相关 14 项 Python、15 项 Web 测试及 TypeScript/构建通过；最终作者修正后还需在冻结源码上运行整套检查。

### 01:05 修正后的作者候选

原生参数键值、Editor 自动保存及参数取值实现的限定只读核对，确认旧作者工程对所有参数硬写 snapEpsilon=0.1，而插值容差实际为该值的 1.5 倍。转身和坐姿有相隔 0.0001 的关键形，过大的容差与导出时错选相邻姿态相符。已按最小键间距的十分之一设置容差，Head/Body/Swing 为 0.00001、Mode 为 0.0025，并增加编辑器显示精度。五视图使用明确的绘制顺序，避免相同 drawOrder 下的原生反向排列。

四个颈部 UV 的进一步核对显示，Editor 首次自动保存把 MODEL_IMAGE 转为 TEXTURE_ATLAS 时重算了这四个网格的 UV；其余 130 个 UV 保持匹配，134 个网格的静态顶点完全未变。这支持预先写入正确图集状态与资源链接的修法；没有据此修改颈部几何。下一次实际导出仍需验证全部 UV。

新候选只使用 `F:\cutemaple\Maple-v4-Export-Ready`，包含 CMO3、23 场景 CAN3、配套 runtime-source、非空原像素图集和完整 handoff-manifest。CMO3 SHA-256 为 `dedfbb1cf768545f23b3bca1d4a61dc193610770acc6709fb7ec9f0af186274b`，CAN3 为 `dfabfcc8df3940fdb6321d9065dd0fd04103d0b8036e91983d24f8da2fca886f`。图集仍为 `78b560c969f63f44799e15a3bb3fe6f89004e8431857abd7cd9b0417fefb75ec`。与首导 IR 比较，只有 86 个部件的 draw_order 改变；形象图像、网格几何和动作曲线保持一致。首次工程的完整 191 文件快照及逐文件哈希在 `assets/authoring/revisions/v4-first-native`。

本轮 51 项作者/关节连续预检/图集/CAN3 测试通过，12 项供应文件校验通过。新增的正式原生 UV 审计工具通过 20 项测试，并对首导真实样本再次得到 130 个匹配、4 个失败，退出码 1；[逐网格原生核对](../artifacts/native-uv-first-export/pairing.json)。这些检查不能替代修正版的真实 Editor 打开与导出。

电脑工具重新激活后能看到 Cubism 当前的旧空白工程，但此前点击和快捷键仍未进入编辑器。已请用户打开 Ready 目录的修正工程，确认角色显示，再把 MOC3、模型设置与纹理一起导出到同目录；没有要求重排图集或断开远程连接。实际修正版原生导出、视觉验收及最终发布仍待完成。

01:11 冻结后的完整 Python suite 为 **269 passed，85.39 秒，零失败/错误/跳过**。155 个相关源码与前端文件的测试前后哈希相同；[最终本轮测试摘要](../artifacts/python-tests-release-gates-freeze-20260909-011112.summary.json)。发布工具现在要求明确的 v4 原生报告、完整且一致的资源哈希和独立逐帧视觉审查，录制成功、旧 v3 报告或缺少实际桌面观察均不能放行；对已有真实诊断的拒绝记录在 [release-gates-diagnostic-rejection-audit.json](../artifacts/release-gates-diagnostic-rejection-audit.json)。此轮没有构建、批准或替换最终安装包。

### 09:29 第二次原生导出

用户已实际从 Ready 工程导出，并保存了 Editor 整理后的 CMO3。完整 38 文件副本和来源哈希保存在 [导出回执](../artifacts/native-v4-editor-20260909-092941/receipt.json)及其 `export-receipt` 目录；复制期间原文件均未变化。新 MOC3 SHA-256 为 `eaf8b90a139eb191f483c8efa8dcacf486ce21c358713af9f52661358b2203bf`，Editor 保存的 CMO3 为 `f70846ed435d8d606832844497e6c6136726fc00de3cae9e44582cb4e720c9bd`。模型设置正确引用新的非空 4096 图集，官方 Core 一致性通过。

本次独立 Core 采样已确认参数容差与排序修复生效：四个视图交界前后的上层透明度约从 0.48998 变化到 0.51002；front 的原生 renderOrder 小于中间视图；BodyTurn=0.358 时正面颈部隐藏、右中间颈部显示；PoseSwing=0.92 或 1 时站立裙腿隐藏、坐姿裙腿显示。原生左攀爬连续录像 246 帧及移动/拖动/落地/睡眠录像 320 帧已保存用于检查；它们仍是诊断证据。

严格 UV 审计仍为 **130/134 匹配，四个非正面颈部失败**。此前仅修正纹理状态的假设没有解决此问题，不批准该 MOC3 发布。进一步限定只读核对 Editor 的纹理更新实现，确认它直接从 CArtMeshSource 的静态 positions 重新计算 UV，不以预先写入的 UV 或当前参数关键形作为来源。这四个网格的静态 positions 使用了注册后的默认形，而源纹理仍使用原始坐标。因此已开始最小修正：静态 positions 与 EditableMesh.point 恢复纹理源坐标，将基线位移保留在标准参数关键形中；所有 9,821 个最终 form 的 XML 与几何保持不变。新候选与其原生导出验证另行记录。

Editor 图集与原作者图集的全部 16,777,216 个 alpha 完全一致，1,775,848 个不透明像素的 RGBA 完全一致；alpha>8 的所有可见像素转换为 8 位预乘颜色后逐通道一致。其余颜色差异集中在极低透明度边缘：alpha=8 的 473 个像素具有非舍入差异，最大预乘贡献差约 7/255。没有重排、翻转或缩放证据，但不能声称原始 RGBA 像素完全相同。坐标兼容审计与颜色变化说明将分别记录，且不能掩盖四个颈部 UV 的实质失败。当前模型仍未替换正式 v3 资源，未构建最终包。


### 09:51 颈部源坐标修正与新导出交付

新工程为 `F:\cutemaple\Maple-v4-Export-NeckFixed\Maple.cmo3`，SHA-256 `6ebc87480f0c58db3156d4d753efb6875d0b0e21f22f7811542671429374aec8`；源工程 SHA-256 `d02e809268309a7f7ca65e989286cfd5124d4cd932a2898cf239016999d66a08`。四个静态 positions 及对应 EditableMesh.point 已修正。恢复这八个数组后，完整 XML 对象图与修改前一致；9,821 个最终关键形的坐标、透明度和绘制顺序未变。从修正 IR 独立重新生成的 259 个 source 也逐项得到相同的最终关键形。不是运行时 UV 补丁。

按 Editor 静态点到纹理输入的计算规则复算全部 134 个网格通过，最大误差 0.000724 图集像素，小于已有 0.001 精度门槛；原图集像素与排版未变。这是源工程预检，尚不能代替真实新 MOC3 的核对。33 个交付文件及精确的源、打包 CMO3 已冻结在 [pre-editor](../artifacts/native-v4-neck-fixed-handoff/pre-editor/)；根任务重新计算交付与归档的全部 SHA256 均匹配。用户导出后保存工程不会丢失原始比对依据。

第二份导出的六段原生连续录制与关键帧审查见 [diagnostic-visual-review.json](../artifacts/native-v4-editor-20260909-092941/preflight/diagnostic-visual-review.json)。左右攀爬、登顶、秋千、移动、拖放、落地、睡眠、清理和互动的抽样画面未发现四处颈部 UV 以外的新增明显问题；该记录明确保留 diagnosticOnly、knownInvalidNeckUV，不能作为最终外观通过证明。

新增完整 134 网格原生 UV 与图集坐标兼容的证据绑定，真实 09:29 失败样本会在完整验收启动及发布前被拒绝。冻结后的完整 Python 测试 **303 项通过，零失败、错误或跳过**；158 个源码及测试夹具哈希前后未变。[完整测试摘要](../artifacts/python-tests-native-uv-gates-freeze-20260909-095225.summary.json)。生产前端本轮未改。

已向用户提供新工程的单次导出请求：无需重建图集，导出模型、模型设置与纹理到 NeckFixed 新目录，并包含隐藏部件和隐藏网格。当前等待实际导出；正式 v3 模型、最终目录构建和桌面长测均未以失败的旧导出替代。


### 09:58 NeckFixed 实际导出通过

用户从 NeckFixed 工程完成真实导出。39 文件原样副本及前后 SHA256 在 [导出回执](../artifacts/native-v4-editor-20260909-095843/receipt.json)；本次所有 33 个交付输入保持原样。实际 MOC3 为 28,623,104 字节，SHA-256 `05c1dd0692ebf626f2452be676ec76b8b102ed4dda9a0271f9bf6debe38cb3e6`。官方 Core 一致性和完整原生 UV 审计通过：**134/134 网格匹配，三角拓扑一致，最大误差 0.000724 图集像素**。[原生 UV 结果](../artifacts/native-v4-editor-20260909-095843/native-uv.json)。独立比较证明相对 09:29 导出仅四个颈部的 UV 及 UV bounds 改变，56 参数、134 网格、30,822 默认顶点、索引、透明度和排序逐值一致。

已通过事务安装器把该模型安装到正式 `assets/live2d/Maple`，旧 v3 保存于 `Maple.backup-20260909T020040.912560Z-904a9332`。完整验收及后续构建均使用安装后的精确资源，包括 `editor-export.json`。新完整 [原生报告](../artifacts/native-v4-final-20260909/full/report.json) 的 refinement、actualNative、ready、passed 均为 true，23 段动作完成，43 次动作/功能捕获有效，25 组参数前后探针通过，errors 为空；测试前、实际载入、结束时完整资源哈希一致。PNG 的坐标兼容审计通过，透明颜色处理差异如前述保留，不伪称原像素完全相同。

6 张 [原生截图审查表](../artifacts/native-v4-final-20260909/full/review-sheets/) 已人工查看，常规动作、站坐替换、手部切换和颈部修正没有发现先前缺陷。强制独立头身大角度的参数压力图仍可出现披帛/饰物侧缘碎片；这些压力组合不是按动作曲线播放的姿态，需按运行时可达范围另行判断。正常登顶在视图交界仍有约 1–2 帧短暂淡化叠影，已明确保留为外观限制，不声称完全无重影。七段连续原生录制和详细审图结果继续保存在本轮 `continuous` 目录。

最终 Core CPU 基准对 23 动作共 2,070 次 update，中位 2.7085 ms、p95 3.4696 ms，全部顶点有限；[基准报告](../artifacts/core-benchmark-v4-final-20260909.json)。不包括 Qt、GPU 或 Framework 物理开销，不能当作桌面帧率。

首轮 v4 Directory 构建在测试前置门槛停止：301 通过、2 项 CAN3 测试失败。原因为测试从当前运行资源生成夹具，却仍硬编码旧 21 动作/48 参数，并在现有 v4 参数后重复追加参数。已仅修测试夹具为明确的独立基础数据，v4 测试独立增加 8 参数和 2 过渡，保留 56 唯一参数身份与 23 场景严格校验；5 项聚焦通过。该修复不改运行时、模型或作者实现，完整正常构建门槛重新执行。尚未批准最终包或完成桌面长测。
