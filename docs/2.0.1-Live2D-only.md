# 2.0.1 Live2D 唯一渲染

本轮仅本地交付，不更新 GitHub。

启动先显示随系统明暗主题变化的加载提示。当前动作完成原生首帧绘制、接点计算并实际进入 Qt 合成画面后显示人物；迟到的实例、动作消息不能显示旧帧。加载期间冻结活动时间，缺资源、超时或渲染失败显示原因以及重试、退出入口。

删除旧 PNG 播放器、精灵图和预览、逐帧缓存/计时/像素命中、Qt 后备粒子层、兼容构建开关及 12 个停用的清理动作。运行模型保留 17 个日常动作和 2 个登顶过渡。清理功能独立，不播放清理动作。

当前 MOC、纹理、原生参数及可编辑素材保留。普通动作隐藏道具的零值曲线和共享表情保留；历史可编辑工程的动作定义仅在 authoring 工具使用，不打入软件。

## 验证范围

- 相关 Python 回归：363 通过、1 项平台条件跳过；播放器：58 通过。
- 生产窗口：加载提示、19 个动作、受控 WebGL 上下文丢失、错误提示及重试。首帧和重试画面已人工查看。
- 生命周期模拟覆盖缺必要动作、旧 token/generation、隐藏、暂停和加载冻结。没有调用需要授权的真实系统清理。
- 左右墙生产播放器连续录制通过：完整 30 秒等待、两个原生周期、面板冻结、接点稳定与头眼微动作。抽样画面已查看。
- 目录包启动与最终 ZIP 校验记录随交付补充。

保留标签 `v2.0.0-before-live2d-only-20260912`；开发分支 `codex/live2d-only-startup`。
