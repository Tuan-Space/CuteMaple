# 2.0.0 本地修复：隐藏联动与秋千轻摆减半

基线 `6d09524`，保存标签 `before-hide-swing-half-20260916`，修复分支 `codex/hide-swing-half`。仅本地交付，不更新 GitHub。

## 修改与回归

- 监控球、简洁网速面板统一受人物可见状态控制；隐藏先关闭显示权限，再取消延迟隐藏和单击，关闭详情。悬停、采样刷新及迟到详情回调不能重新显示控件。显示时恢复常显偏好和当前鼠标对应的控件，不重开详情；隐藏期间网络采样和清理结果处理继续。
- 轻摆幅度从 0.2 减至 0.1，抚摸及恢复使用同一目标；大摆幅度、3.87 秒轻摆周期、1.2 秒过渡、连续相位及原生参数精度不变。
- `python-tests.log`：218 项通过，包括新增的隐藏、迟到回调、真实 Qt 计时器、常显切换和暂停恢复测试。`additional-tests.log`：18 项通过，覆盖应用、监控采样、渲染生命周期与攀爬计时。
- `web-tests.log`：58 项通过。生产 Core 模型在 30/60/144 Hz 和不均匀帧时间下各完成十个轻摆周期，检查实际网格经过中心无停顿，回归抚摸、相位连续和暂停恢复。
- `desktop-ui.json`：正常 Windows Qt/WebEngine 记录 632 帧，包含 40 秒轻摆、大摆转轻摆、抚摸、暂停、隐藏和恢复。66 次隐藏检查中人物、两个监控控件、详情及气泡均不可见；轻摆稳定幅度为 0.1，暂停和隐藏期间相位不变。`swing-review.jpg` 为实际播放器黑白及棋盘背景画面；清理反馈为受控注入，没有执行真实清理。

## 模型与源码包

运行 MOC 和纹理未修改，无需 Cubism 导出。工作区原有、未提交的 `assets/authoring/model/Maple.cmo3` 原样保留，SHA256 为 `d44c5856b839a716beb2b8d4e55b08254010c0e02d565f321494343ef5e8328a`，未纳入本次 Git 提交。

该未提交工程含编辑器本机 PSD 绝对路径，不能作为已验证可移植的源码交付。源码 ZIP 使用当前 Git 已保存的完整模型工程（SHA256 `69f1b00653fb6f94174d353d03442c29a1419bee0dbdc216c837c9a59ea9a876`）及本次提交的最新代码，其余完整素材照常保留。工作区原件没有被替换。

## 验证边界

最终目录包的启动、资源一致性和压缩包校验另附 JSON 记录。历史制作链测试仍有依赖已删除旧工程的项目，构建按开发文档跳过该旧链，使用上述相关回归。本轮不更改清理助手协议，不执行真实 UAC、系统清理或长期运行测试；这些仍由用户自测。
