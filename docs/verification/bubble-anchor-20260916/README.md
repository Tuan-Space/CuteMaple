# 2.0.0 本地修复：攀爬与秋千气泡靠近人物

基线 `7d479d1`，保存标签 `before-bubble-anchor-20260916`，分支 `codex/bubble-head-anchor`。本轮按用户最新要求保持上下布局，不采用左右侧边方案。

## 最终行为

- 攀爬气泡位于实际头顶上方，间距 8 个 Qt 逻辑像素，因此比按透明窗口定位时更靠下、更贴近人物。
- 秋千气泡位于可见人物下缘下方，间距 8 个逻辑像素，因此比按整个窗口底部定位时更靠上。水平跟随头部居中，在屏幕边缘限制范围。
- 只有首选垂直方向空间不足时才尝试另一垂直方向；不放到左右侧边。位置随当前 generation/token 的模型几何、窗口移动、人物缩放和屏幕变化更新，不重置气泡期限。
- 单击、双击和清理反馈使用同一定位方式。保持气泡优先级、主题及隐藏规则，补充阻止隐藏后迟到的普通对话显示。
- 复用当前 MOC、纹理、动作及 Web 播放器，无需 Cubism 导出。清理源码未修改。

## 验证

- `python-tests.log`：207 项通过，覆盖头顶／人物下缘定位、屏幕四角、负坐标屏幕、人物缩放、长文本、迟到渲染消息、清理气泡期限和隐藏恢复，以及监控控件隐藏联动。
- `desktop-*`：生产 Qt/WebEngine 的实际模型与气泡画面，包含左右墙等待及完整原生攀爬、左右登顶过渡、大摆、轻摆、地面及人物缩放。点击由测试程序注入，攀爬等待由测试程序缩短；没有更改正式计时或执行系统清理。高 DPI 检查通过 Qt 缩放因子完成，不修改 Windows 显示设置。
- 最终编译输入、运行资源、短启动和 ZIP 校验记录随本目录及交付包保存。相关检查不替代实际 UAC、系统内存清理或长期运行。
- 100%、125%、150%、200% Qt 缩放分别检查 249、152、242、199 帧，共 842 帧；编译后的播放器完成 19 组动作检查和普通桌面短启动。

## 本次交付限制：暂不包含内存清理助手

构建最初复用了原有助手，没有重新编译。包检查运行助手诊断时，Windows 防护拦截并隔离了该文件（`Trojan:Win32/Cloxer`，详见 `windows-detection.json`）。因此完整清理烟雾检查未通过，不能宣称内存清理可用。没有恢复隔离文件、添加白名单或修改防护设置。

用户明确选择先交付气泡修复、暂不包含清理助手。最终程序包移除整个 `cleaner` 目录，内存清理暂不可用；内存监控、网速与桌宠功能保留。现有程序在检查到助手缺失后结束清理请求并显示气泡，不发起 UAC。源码包继续包含清理源码，便于后续修复；它不是已验证可用的清理二进制交付。

## 保存边界

未提交的 `assets/authoring/model/Maple.cmo3` 原样保留，SHA256 为 `d44c5856b839a716beb2b8d4e55b08254010c0e02d565f321494343ef5e8328a`；源码 ZIP 使用 Git 中已验证可移植的完整工程，SHA256 为 `69f1b00653fb6f94174d353d03442c29a1419bee0dbdc216c837c9a59ea9a876`。不更新 GitHub，不修改个人设置或 Windows 防护。
