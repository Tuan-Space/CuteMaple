# 手账 2.2.4 本地验收

基于 2.2.3 `aac7e13`，分支 `codex/journal-interaction-2-2-4`。保留清爽浅蓝、深色主题和内置雅痞标题字体。仅本地提交，不推送。

## 完成内容

- 笔记回收站保留可选择、复制的只读预览及已有附件查看、播放；隐藏格式、添加附件、录音、新建与保存状态，拦截快捷键、粘贴和拖放写入。
- 两个回收站使用相同的右侧勾选、全选当前结果、选中数量、恢复和永久删除按钮。全选包含未加载的结果；分页与刷新保留勾选，筛选和离页清空。正文点击与勾选独立，空格切换当前行，空态不含勾选框。
- 恢复与删除按钮的尺寸、边框、背景、禁用态一致；删除以危险色文字区分。单项右键仅处理被右击记录，批量操作不借用当前预览。
- 回收站按删除时间倒序，整计划与单次发生使用不同键；批量数据库更改统一事务，失败回滚。过期未完成、需要改期或会立即提醒的项目保留待处理，页面提供处理入口。
- 普通笔记列表和原生文字菜单可永久删除。删除当前笔记前等待保存、录音和附件收尾，之后清除计时器及身份；其他笔记的删除不打断当前编辑。附件清理在事务完成后执行一次，保护其他笔记、原稿与备份引用，清理失败单独反馈。
- 笔记工具栏按组换行，整合段落样式、粗斜体、列表、引用链接、回形针菜单、矢量撤销重做与模式选择。源码模式保留必要入口和源码撤销重做；删除保存成功时间及占位，保留失败和处理中信息。
- 提醒编辑器采用分组单页、控件上方标签、两行日期时间、两列纪念日选项、响应式提前提醒、高级设置与独立预览。保存取消固定底部，校验错误可见并定位相关字段。已完成和已删除提醒为真正的只读摘要。
- “今天”“提醒时段”“更多设置”“发生记录”等操作有明确按钮样式。健康设置保存成功静默更新，失败仍提示。

无数据库迁移；保留原 Markdown、原稿备份、媒体与业务接口。

## 截图检查

`screenshot-manifest.json` 列出 **518 张唯一界面截图**：浅／深色 × 100%／125%／150%／200%。五个主页面及主要编辑器覆盖正常窗口与 620×420，小窗口检查顶部及滚动底部。100% 两主题补齐辅助弹窗、附件、复杂正文、源码、录音各状态、播放完成、搜索无结果及错误状态。

所有截图使用隔离资料库，固定日期 2026-10-17，准备 119 篇笔记、长标题、附件、混合提醒、同计划的多个单次删除记录、已完成及过期项目。没有打开个人资料库。批量恢复截图执行实际恢复，剩余项目与提示数量一致。

每个截图目录的 `report.json` 记录逻辑尺寸、字体、横向溢出及播放检查。`icon-recapture.json` 记录放大审阅发现图标问题后重拍的编辑器子集。`visual-review/` 保存逐文件审阅清单与复验结果。

逐图检查修正了回收站列表过窄、全选与半选标记不清、展开符号缺字和图标透明背景未初始化问题；另以回归测试修复了 Qt 隐藏勾选热区造成的显示／选择不一致，以及从总览返回笔记时回收站状态覆盖目标笔记的问题。

录音状态截图使用模拟状态，不访问麦克风。播放检查使用生成的短静音 WAV，未采集真实声音。

## 测试与编译验证

- `regression.xml`：最终 **318 项通过**，无失败、错误或跳过；覆盖应用、存储、格式、媒体、布局、主题及生命周期。
- 新测试覆盖超过 100 条结果的全选、跨分页、筛选与离页清空、单项与批量范围、取消删除、待处理恢复、真实 SQL 失败回滚、附件引用保护、单次排除标记、只读输入拦截、格式往返和删除后独立数据库连接验证。
- `compiled-light/`、`compiled-dark/`：最终编译版两主题 **32 张截图全部复验通过**，字体、矢量图标、只读回收站、105 条全选、源码工具栏、固定校验错误、真实静音播放及 Live2D 独立运行均通过。截图工具等待布局完成后再取图，避免缩放和模式切换中的瞬态布局。
- `SOURCE-SNAPSHOT.json` 与 `source-match.json`：**35 个运行模块、53 个运行资源**与编译输入逐一 SHA256 匹配。
- 安装器编译成功，窗口标题显示 2.2.4；窗口自动化两次返回 `failed to activate captured window`，未取得向导有效截图，未继续向导或安装。`installer-check.json` 记录此限制，文案按本次编译安装脚本核对。完整源码包另核对文件哈希、模型资源和嵌入纹理完整性。

完整历史制作链仍缺少 `assets/authoring/revisions/v5-transfer-fixed/layers.json`；旧双击表情断言与 2.2.2 已改为双击打开手账的行为不符。沿用 2.2.3 已验证的应用回归范围并加入本轮测试，没有将这些历史入口记为通过或改动人物行为。

## 模型稿与交付

未提交的 `assets/authoring/model/Maple.cmo3` 编辑稿保留，SHA256：`d44c5856b839a716beb2b8d4e55b08254010c0e02d565f321494343ef5e8328a`。

源码 ZIP 使用已提交、与运行模型对应的 CMO，SHA256：`69f1b00653fb6f94174d353d03442c29a1419bee0dbdc216c837c9a59ea9a876`。交付根目录包含安装 EXE、完整源码 ZIP、各自 SHA256、合并校验清单和本地交付记录。
