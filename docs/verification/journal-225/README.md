# 手账 2.2.5 本地验收

基于 2.2.4 `4d5eb83`，分支 `codex/journal-detail-2-2-5`。本地提交，不推送。

## 完成内容

- 浅色改为浅灰背景、白色内容区，深色改为炭灰背景；蓝色用于主要操作与焦点。保留雅痞标题字体，正文与密集数据使用清晰阅读字体。条目分别承载标题、摘要、日期和重点状态，使用留白与细分隔线。
- 进行中提醒按有效安排时间升序，在全部匹配记录排序后分页。当前待处理发生记录优先，否则寻找下一次有效安排，跳过已完成、取消、排除日期；历史漏回应不挡住未来安排，没有未来安排时回退最近未处理记录。通知调度逻辑不变。
- 提醒条目突出“还有 N 天”“今天”“已过 N 天”；当天已过时刻的未处理项为“今天 · 待处理”。按规则时区的自然日计算，条目只显示日期，完整时刻留在详情和发生记录。
- 普通笔记按创建时间倒序，显示创建日期；修改正文、自动保存、添加附件不会改变创建排序。
- 普通笔记、进行中提醒支持右键置顶和取消置顶。置顶组优先，组内保留日期排序，ID 提供稳定同值排序。置顶写入现有 `meta`，随资料库备份；回收站和完成列表不应用置顶排序，恢复保留设置，永久删除事务内清理。
- 页面打开、窗口激活和 30 秒定时检查更新倒数与顺序，并保留选中项和滚动位置。跨日更新最多延迟一个检查周期。
- 普通新建提醒使用下一次 10:00:00；日历新建保留所选日期并使用 10 点。已有时间和切换类型后的用户输入保持不变。公历显示年、月、日单位，时间两位补零并带时、分、秒。
- **农历日期保留“初一、初八、十五、廿九、三十”等自然写法，不添加“日”，避免“初八日”。** 字段上方仍有“日”标签；中文月份、闰月和公农历同一实际时刻转换保留。此项按用户截图反馈修正后重拍全部受影响的编辑器截图。
- 笔记工具栏固定单排，引用、链接、编辑模式使用矢量图标；正文样式、模式保持可达，空间不足时其余工具按组收进更多菜单。菜单复用按钮命令、状态与禁用规则，保留编辑选区。源码模式和只读原文保护继续限制适用操作。
- 笔记回收站展示标题、最多两行纯文本摘要、独立小字号删除日期；去掉 Markdown 和附件路径。普通笔记和健康统计取消重复类别标签；附件突出文件名，类型、大小、打开方式作为辅助说明。
- 通用确认框加载 Qt 中文翻译，按钮使用“是／否／确定／取消”。提醒编辑器仍为分组单页，底部错误与保存、取消保持可见。

无数据库迁移；保留 Markdown 存储、原稿保护、自动保存和媒体生命周期。

## 截图与交互检查

`screenshot-manifest.json` 包含 **638 张源码界面截图 + 36 张最终编译版截图，共 674 张**，逐文件附 SHA256。`visual-review/` 保存 38 张审阅总览及审阅说明。

源码覆盖浅／深色 × 100%／125%／150%／200%，五个主页面与主要编辑器覆盖正常和 620×420 逻辑窗口，检查小窗口顶部与滚动底部。100% 两主题补齐辅助弹窗、空态、搜索无结果、错误、回收站多选／全选、置顶、模式与溢出菜单、附件、复杂原文、播放结束及录音状态。

全部截图逐张在总览中审阅；提醒日期、倒数、置顶、回收站元信息、工具栏、附件与小窗口另做关键截图原尺寸检查，不将总览审阅描述为每张均原尺寸打开。检查后修正了引用图标辨识度、英文确认按钮和农历多余“日”，并重拍相关页面。当前未发现未解决的裁切、横向溢出或信息层级问题。

使用隔离资料库、119 篇混合笔记及混合提醒，固定源码截图日期 2026-10-17；覆盖长标题、附件、已完成、过期和同计划多个单次删除记录。所有八组 `report.json` 通过，横向溢出为零。`icon-recapture.json` 记录最终编辑器重拍及农历文案断言。

录音状态截图为模拟状态；实际播放使用生成的短静音 WAV，检查自然结束、外部点击收起与释放。未访问麦克风或个人资料库。

## 回归与发行检查

- `regression.xml`：328 项通过；`detail-regression.xml`：后续定向回归 66 项通过；`lunar-final-regression.xml`：最终农历修正及界面回归 41 项通过。按测试名称去重共 **335 项通过**，无失败、错误或跳过，详见 `regression-summary.json`。不是将三个批次的重复用例相加。
- 新增测试覆盖超过 100 条记录的排序／分页、置顶搜索／持久化、编辑旧笔记不移位、恢复与永久删除、SQL 失败事务回滚、备份保留置顶、提前／稍后提醒不改安排排序、135 个排除日期、跨年与夏令时自然日倒数、10 点前后默认值、已有时间、公农历切换、单排工具栏与更多菜单、源码／只读限制、纯文本摘要及中文确认按钮。原有格式保存重开、媒体生命周期、附件保护与回收站回归继续通过。
- `compiled-light/` 与 `compiled-dark/`：最终 Nuitka 编译 EXE 两主题通过，36 张截图完成复核；字体、矢量图标、105 条全选、只读状态、源码工具栏、可见错误、置顶、10 点默认、短音频播放、Live2D 首帧及独立运行均通过。编译版使用系统显示缩放，完整四档缩放矩阵由源码验收覆盖。
- 最初沙箱运行仅 Live2D 首帧未就绪；在允许桌面渲染的执行环境中用全新隔离资料库复验，两主题均无错误。交付的是最终成功报告。
- `SOURCE-SNAPSHOT.json` 与 `source-match.json`：35 个运行模块、53 个运行资源与最终编译输入逐一 SHA256 匹配。
- Inno Setup 安装器编译成功，版本 2.2.5，自启文案为“开机自动启动”。`installer-check.json` 记录安装器哈希；验收未覆盖现有安装，也未宣称实际完成安装向导。完整源码 ZIP 另做逐文件哈希与模型资源完整性检查。

历史制作链仍缺少 `assets/authoring/revisions/v5-transfer-fixed/layers.json`；旧双击表情断言与 2.2.2 已改为双击打开手账的行为不符。本次沿用已验证应用回归范围并增加本轮测试，未将这些历史入口记为通过。

## 模型稿与交付

未提交的 `assets/authoring/model/Maple.cmo3` 编辑稿保持原样，SHA256：`d44c5856b839a716beb2b8d4e55b08254010c0e02d565f321494343ef5e8328a`。

源码包使用已提交、与运行模型对应的 CMO，SHA256：`69f1b00653fb6f94174d353d03442c29a1419bee0dbdc216c837c9a59ea9a876`。交付目录包含安装 EXE、完整源码 ZIP、合并 SHA256、源码验证报告及提交号记录。源码与编译输入通过哈希对应，截图、测试和本记录均随源码提交。
