# 2.1.0 原生清理助手：本地验收

本轮不上传 GitHub。基线 `9dccad1`，保存标签 `before-native-cleaner-20260916`，分支 `codex/native-cleaner-2-1-0`。

## 已完成

- 清理助手改为 C++／MSVC x64 原生程序，静态链接 C++ 运行库；运行时只需要 Windows 系统组件。固定版本 JSON 依赖和许可证随源码提供。
- 保留会话、监督、单步进程结构，管道协议 1、记录 schema 4、六个步骤名称及 15／45／60 秒期限。保留真实首次 UAC，后续请求复用同一会话；进度及结果仍使用人物气泡。
- 原助手没有恢复隔离或加入白名单。原 Python 实现仅保留为明确命名的测试参考文件，不进入正式程序或生产清理入口。
- 修复重复点击与助手接单同时发生时，把短暂文件锁占用误报为失败的问题。进程身份、文件目录防替换、助手镜像锁定、固定子进程及退出核验保持有效。
- 当前模型、纹理和动作未改变。用户未提交 CMO 原件 SHA256 仍为 `d44c5856b839a716beb2b8d4e55b08254010c0e02d565f321494343ef5e8328a`。

## 验证结果

- Python 相关回归：279 通过、1 跳过。跳过项需要当前账户创建测试符号链接；没有为测试修改系统策略。历史参考模型测试不代替原生进程测试。
- 原生测试程序：15 个场景通过，包括正常／缓慢完成、部分失败、工作进程崩溃、15 秒超时、取消、错误令牌、错误请求身份或哈希、过期请求、非允许步骤、监督进程与会话进程崩溃、外部进程连接及客户端崩溃。该独立测试二进制不能调用真实清理 API，也不进入交付包。
- 最终程序包：启动、19 组 Live2D 动作、助手无权限诊断通过；正常 Windows 图形后端桌面运行 15 秒并正常退出。
- 完整可编辑素材检查通过：147 个分层、149 个纹理引用、163 个 CMO 内嵌纹理、两份 CAN 工程和 19 组运行时动作，无本机外部文件依赖。源码包使用 Git 中的可移植 CMO，保留用户工作区原件。

## 三次真实清理

通过最终编译程序的普通桌宠入口发起，用户手动确认一次 UAC。三次使用同一个会话，每次快速重复点击均未额外创建操作；实际记录目录只有三个 operation ID。

| 次数 | 六项返回 | 总耗时 | 可用物理内存变化 |
| --- | --- | --- | --- |
| 1 | 全部成功 | 13.328 秒 | +7,855,128,576 字节 |
| 2 | 全部成功 | 4.078 秒 | +1,578,979,328 字节 |
| 3 | 全部成功 | 2.704 秒 | +257,327,104 字节 |

以上是执行期间的实际内存变化，不代表每次使用可释放固定容量。18 项原生调用及退出码重新核验通过，没有触发保护期限；工作进程、监督进程及会话均已退出。原始事务文件保留在 `F:\cutemaple\清理记录-2.1.0\真实清理验收\profile\cleanup`，文件哈希由 `real-cleanup.json` 绑定。

## 防护与交付边界

最终主程序 SHA256：`fa6b3d0e679d1967abaaf37f872045b28be4f9d7e7dc489fc9b9877ed156e602`。

最终清理助手 SHA256：`2ed5fad32b7987c752849cff1dcae1925178222972e7b12bccef8588913ab003`。

最终包扫描和真实运行期间，Defender 实时防护、行为监测保持开启，当前签名版本 `1.459.218.0`，未观察到针对该包的新检测。`initial-scan.json` 属于早期原型；最终依据是 `package-scan.json` 和 `post-cleanup-protection.json`。这些结果仅说明本机本轮验证通过，不保证未来病毒库或所有电脑的检测结果。

本轮没有执行长期稳定性测试或重新制作人物动作。现有气泡定位、隐藏和轻摆改动通过相关回归，运行模型哈希保持不变。
