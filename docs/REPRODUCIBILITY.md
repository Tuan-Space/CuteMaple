# 仓库整理与独立复现验收

验收日期：2026-09-18。基线：`d8fb716`。对应发布：2.2.7。

## 整理范围与删除依据

| 项目 | 整理前 | 整理后 |
| --- | ---: | ---: |
| 根目录普通文件 | 48 | 6 |
| 完整源码包文件数 | 3,625 | 约 642，含逐文件哈希与提交信息 |
| 完整源码 ZIP | 314.6 MB | 约 100.8 MB |

36 个程序模块移至 `src/`，4 个开发脚本移至 `scripts/`，3 份依赖清单移至 `config/`。静态导入与动态入口审计确认 36 个运行模块均可达，没有按文件名猜测删除业务代码。

当前树移除 2,984 个历史验收文件（209,350,438 字节）、4 份过时说明和一个约 34 MB 的原生打包前中间 CMO。历史验收文件与中间模型保留本地副本，Git 历史和旧 Release 保留。中间 CMO 已由保留的 `Maple.rig.json` 重新生成，通过 9 项图集转换测试；不能替代的源画、分层 PNG、当前 CMO/CAN、纹理、动作与制作工具继续保留。

README 保留功能介绍与原有风格，仅补充目录和完整源码获取方式，不加入版本更新记录。新生成的截图、日志与验收产物写入忽略目录，不重新塞回源码树。

## 独立源码与编译输入

源码 ZIP 从指定 Git 提交生成，使用提交对应的 LFS 对象，不读取工作区模型草稿。归档中有 `SOURCE-REVISION.json` 和 `FILE-HASHES.json`；缺文件、LFS 指针、哈希错误或模型包外依赖会中止验收。

源码在原仓库之外解压，创建独立 Python 环境，以 `pip --no-cache-dir` 安装固定依赖；前端通过 `npm ci` 从锁文件重装并重建。随后成功编译 Nuitka 程序、MSVC 原生助手和 Inno Setup 安装器。没有借用原仓库的虚拟环境、未提交文件或模型编辑稿；公开编译工具使用已安装版本，具体版本与获取地址见 [开发指南](DEVELOPMENT.md)。

编译快照中的 36 个 Python 模块和 53 个资源与提交输入的 SHA256 相同；整理前后核对的 178 个运行模型及制作素材文件保持原哈希。源码包使用已提交 CMO，SHA256 为 `69f1b00653fb6f94174d353d03442c29a1419bee0dbdc216c837c9a59ea9a876`。本地未提交 CMO 的 SHA256 仍为 `d44c5856b839a716beb2b8d4e55b08254010c0e02d565f321494343ef5e8328a`，未上传。

完整性检查覆盖 19 个运行时动作组、147 张分层输入、149 个结构化纹理引用、163 张工程内嵌贴图及两份 CAN 动画工程。另修复第三方制作模块校验清单遗漏的既有精度修改，以及回归测试依赖旧目录、历史生成报告的问题；测试直接检查保留的源素材和当前制作链。

GitHub 隔离克隆从远程整理分支取回源码并执行 `git lfs pull`，`git lfs fsck` 和上述模型引用检查均通过；该克隆未使用原工作区的 LFS 缓存。

## 回归与界面

- Python 回归按分组运行，对修正项复跑后最终覆盖 1,530 项通过、1 项环境跳过（当前测试权限不能创建 Windows 符号链接）。此统计按测试身份去重，包含修正后的复跑结果。
- 前端重建后 58 项测试通过。
- 原生助手模拟版 15 个场景通过，含成功、失败、取消、超时、错误令牌、异常退出与身份校验；未执行真实清理。
- 从其他工作目录启动源码通过，记录退出码为 0。
- 编译版五个手账页面、回收站 105 条跨页全选与只读、工具栏图标、置顶排序、10 点默认值、附件与静音播放结束检查通过。
- 检查浅色 100%／125%、深色 150%／200%，包含正常窗口、小窗口回收站、只读详情和错误提示；截图审阅未发现本轮路径整理造成的字体、图标或布局退化。本次没有重新改变界面设计。
- 编译版运行 350 秒桌面场景，包含暂停恢复、隐藏显示、尺寸变化、动作切换、两侧攀爬及顶部秋千；无错误，退出码为 0。物理拖拽、键盘／音频互动和显示器热插拔未在本轮人工复验。
- 安装器在新临时目录安装后通过同样的页面与播放检查；卸载完成，隔离资料库保留，原有开机启动值与快捷方式恢复。

详细截图和运行日志保留在本地验收目录；本文件及 Release 的简短验收记录用于公开交付。回归结论只覆盖已运行的场景，不把手动硬件交互、麦克风录音或系统清理视为本轮已测试事项。

## 模型编辑与原生导出

用户在 Cubism Editor 5.3.04 中完成隔离模型的参数预览、恢复、另存与 MOC 原生导出。另存 CMO 可重新解析；导出 MOC 通过官方 Cubism Core 一致性与几何检查，80 个参数及 159 个网格 ID、参数范围、默认值、三角形索引均与现有运行模型一致，默认姿态最大顶点差为 0。贴图与物理配置 SHA256 与已发布运行输入相同，导出资源引用均位于隔离目录内。

这次原生导出验证了完整源码内的可编辑工程能够继续使用；验收副本不替换正式运行模型，不覆盖或上传本地未提交草稿。模型编辑与导出这一最后待验步骤已经完成。
