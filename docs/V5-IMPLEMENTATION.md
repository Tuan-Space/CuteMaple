# v5 implementation and acceptance record

## Preserved baseline

- Local v4 snapshot: `a23d1ab`, tag `v4-candidate-20260909`.
- Work branch: `fix/v5-motion-interaction`; no remote push authorized.
- Original Editor directory copied byte-for-byte into `assets/authoring/archives/v4-neckfixed-export-20260909`; 39 files, 71,192,722 bytes, per-file hashes in `ARCHIVE-SHA256.json`.
- Existing v4 distribution and share ZIP remain rollback artifacts. Personal settings (including user-enabled roaming/autostart) must not be reset.

## Approved implementation

- Six real memory cleanup operations; fix Kernel32 binding and registry information class 155. Operation-specific requests, acknowledgement, cross-process exclusion, supervisor and fixed-step workers, owned Job Object, verified process exit. Guards: step 15 seconds, work 45 seconds, task 60 seconds, UI observation 65 seconds. Failure must never produce success animation or update last-success time.
- Seated curled sleep; shared hand/cuff chains; original ribbon opacity with cutout defects repaired; bent-knee climbing tied to authored support phases; collar/jaw neck attachment; accelerating fall and independent ribbon wind; fan-based cleanup gestures with supporting hands preserved.
- Slightly stronger head gaze and small ground-only body gaze. ToDesk-compatible activity-only keyboard detection; audio output selection, level and status. Same-WebEngine particle layer, previews, 24-particle limit. Details panel freezes displacement while input responses continue. Explicit pause still freezes activity; dragging while paused samples wall/top poses without advancing time.

## Acceptance gates (pending)

- New compiled cleaner: three actual successful sequential runs, all six steps, matching operation IDs, verified exits, and duplicate-click handling. Controlled workers cover timeout/crash/cancellation; do not hang real system calls for a test.
- Native Cubism export of the v5 authoring project, with source/atlas/native UV alignment checked. Review continuous motions and transitions on black, white, checkerboard and desktop backgrounds; parameter activity alone is insufficient.
- Actual ToDesk typing, actual audio output and endpoint switching, simultaneous typing/audio, paused wall/top dragging. Preview effects are not input evidence.
- Relevant regression suites, 20 launch/exit runs and 60-minute ordinary desktop run of the same final package under enabled Windows protection. Do not mark release passed while required observations remain missing.

Implementation is in progress. No v5 package has passed these gates yet.

## Implementation evidence (2026-09-09)

- Cleanup backend: fixed six-step dispatcher, schema 4 operation files, OS mutex, worker Job Object and verified exits implemented. The 41 focused backend checks passed, including benign real Windows child lifecycle checks. No real system API was deliberately blocked.
- New standalone helper compiled at `.build/cleaner-v5-20260909-174103/out/cleanup_helper.dist/CuteMaple-Cleaner.exe`, SHA256 `27de45217f40fe285e71ada3f8dd0869abeec6ba09d9d0fb5e30776d6edd333a`. Its ordinary-user diagnostic exited 0, imported no Qt, and performed no privileged operation. Source snapshot is adjacent to the build stage.
- Actual UAC installation attempt for the isolated profile returned Windows error 1223 (authorization cancelled). Evidence: `artifacts/v5-cleaner-acceptance/install-01/report.json`. The isolated task did not exist before or after; no cleanup was scheduled. Do not retry elevation automatically or describe the three-run gate as passed. The personal-profile v4 task was preserved.
- Host now schedules cleanup/authorization checks outside the GUI thread, gates success on verified exit, preserves interrupted drag/fall placement when results arrive, and keeps unresolved operations busy while polling. Details no longer suppress keyboard/audio responses. Paused wall/top dragging samples a native still pose. Falling accelerates and provides velocity context; native climbing uses authored phase travel rather than a second movement clock.
- The full initial source suite had 388 passes and one test-isolation failure. The latter was caused by the app test module's cleanup-disable environment variable; the isolated fixture was corrected and its combined regression passed. Further tests and the final native model are still in progress.
- Desktop QA now hashes the entire final distribution before/after its run, including the standalone cleaner and native dependencies, in addition to the renderer resource checks. No v5 20-launch/60-minute report exists yet.
- Cubism has been reopened. Fresh UI observation confirmed that click and Open actions can reach this Editor instance; retry native export directly when the reviewed v5 project is ready.

## Additional implementation verification

- The host's complete entry/loop/exit cleanup lifecycle passed 74 focused app/integration checks (`artifacts/v5-host-clean-segments-02.xml`). The eight internal clips preserve the 21 public actions. Finishing a cleanup while dragging or falling preserves the current placement; stale completion tokens cannot finish a newer animation.
- Final cleanup-backend and evidence-tool regression: 63 checks passed (`artifacts/v5-cleanup-evidence-tests-final.xml`). The client acknowledgement window is at most 10 seconds within the unchanged 60-second supervisor budget. Each real native step now retains structured return values and owned-process exit evidence. The earlier `27de...` helper predates these receipts and is obsolete for final acceptance; rebuild the helper in the final distribution.
- A real ordinary-user activity preflight exposed audio child exit 120: Chinese endpoint names were encoded using the Windows pipe locale while the parent expected UTF-8. The IPC now uses ASCII-escaped JSON without changing the decoded names. The corrected native preflight enumerated USB Audio/HID and ToDesk Virtual Audio, retained a live meter, and stopped both child and keyboard hook cleanly. The default USB endpoint was reported muted; no sound, typing, or device change was invented as a passing interaction (`artifacts/v5-activity-provider-preflight-fixed-final/report.json`).
- Continuous review tooling now supports actual black, white and checkerboard backgrounds and all four cleanup postures' entry/loop/exit clips. Both the main desktop run and supplementary observer bind the entire distribution, including cleaner and DLLs, before and after execution.

## Native review and additional defects

- Full source regression before the latest native corrections: 535 passed, no failures or skips, 309.16 seconds; all 104 Python source hashes were stable (`artifacts/v5-full-suite-final-01.xml` and its summary). Runtime code is saved locally at checkpoint `7318d50`; this is not a release acceptance commit.
- Cubism opened the new 31-scene CAN3 and played the idle and clean_top timelines. The bounded Editor observation is recorded in `artifacts/native-v5-handoff/editor-can3-review.json`; it does not claim all scenes were visually accepted.
- Two actual Cubism MOC exports were preserved. Both Editor texture modes changed a small number of original alpha values. The runtime installer now allows the exact authored atlas only after all 141 native UV pairings and source hashes match. The first export paired within 0.0007241 atlas pixels; the restored texture is byte-for-byte the original atlas. Editor output files remain unchanged.
- Actual native QA at `artifacts/native-v5-runtime-03` completed all 31 clips and 67 captures. Sleep proportions, rest contacts, independent ribbon wind and clip identity passed. Climb and fan checks failed because some sleeve/hand contacts separate, with a worst gap of 0.03061 canvas units. Independent UV interpolation confirmed a real rig defect; these results are not a passed release report.
- Continuous playback at `artifacts/native-v5-visual-03` additionally exposed drag/fall arm motion hidden by the resting-hand mode. Both the contact deformation and active-hand selection are being corrected before a new export and acceptance run.
- Host review identified cancellation during the background request Future and interrupted sleep state as separate races. Both were corrected and saved at local commit `8cd9bc3`; 82 focused tests passed (`artifacts/v5-cleanup-host-lifecycle-tests.xml`). Pending cancellation is retained until a late operation arrives, a late authorization-required result cannot open UAC after cancellation, and the start of a cleanup pose clears the interrupted sleep state.
- The subsequent full source regression passed 549 tests in 387.80 seconds, with no failures, errors or skips and all 124 tracked test/source inputs stable (`artifacts/v5-full-suite-final-02.xml`, SHA256 `508dd4c2afe14d01a043998377b494a090d0e9a449e391b92e2ade682ce1f562`). Later authoring/native-contact checks are recorded separately; this is still not final-package acceptance.
- Native ContactFixed export 04 substantially reduced wrist gaps, but three wall-to-rope transfer contacts still exceeded the unchanged 0.012 canvas limit. Their shoulder compensation used the old wall wrist after the hand moved toward the rope. TransferFixed synchronizes that affine field with the existing wrist path while leaving the shoulder and hand path unchanged. All 171 actual capture/probe parameter sets were replayed, with 348 visible contacts checked before export 05. Earlier failed exports and reports remain unchanged.
- The corrected native QA explicitly checks active relaxed hands during left/right drag, falling and the start of landing, while retaining five resting motions, four independent rest probes and the original sleep/visibility/gap criteria. The release evidence validator independently checks these actual captures; 93 related tests passed (`artifacts/v5-active-contact-release-tests.xml`).
- The obsolete shared Editor receipt accidentally written during isolated export 02 was preserved at `artifacts/native-v5-export-02/global-editor-export-legacy.json`. The canonical receipt was restored to the v4 baseline while its active runtime remains v4. New isolated installs keep their own exact receipts inside their runtime directories.

## Final native candidate integration

- Actual Cubism export 05 of TransferFixed passed the full native refinement run: 31 motions, 68 captures, all nine named v5 checks, no reported errors (`artifacts/native-v5-runtime-05/report.json`). Rest and active drag/fall hand contacts are separate measurements within the same contact check. The accepted MOC3 SHA256 is `5cd77005ea7c5ac9ef02a661d02a30334003d69264e30c3dbaadcfc5cd237ed1`. All 141 native UV pairs matched within 0.0007241 atlas pixels; the installed atlas remains the exact original `93f372...ebb2b` image.
- The exact isolated runtime directory, including its receipt, was promoted byte-for-byte to `assets/live2d/Maple`; the previous active v4 directory is preserved at `artifacts/v4-active-runtime-before-v5`. The canonical authoring receipt now matches the promoted runtime receipt. Inventory validation passed. No model/frontend bytes were regenerated during promotion.
- The complete external TransferFixed Editor directory was copied and hash-checked into `assets/authoring/archives/v5-transfer-fixed-export-20260909`: 46 original files, 76,574,246 bytes, with an additional archive manifest. CMO3, CAN3 and original native Editor output remain available. The CAN3 retains its absolute authored CMO3 reference; relocating it requires relinking in Cubism.
- The final CAN3 opened and played idle in Cubism with observed frames 0, 197 and 279, then was stopped. This is a bounded editable-project check, not a claim that every Editor scene was played. The native runtime run covers all 31 clips separately (`artifacts/native-v5-transfer-fixed-handoff/editor-can3-review.json`).
- A final interaction review found that the host previously sent only one ambient expression despite emitting both particle types. Keyboard/audio expression targets now remain independently active; keyboard retains gaze/status priority. Independent stopping, explicit-expression ownership, blocking, pause/hide, late events and renderer reload were covered by 90 passing checks. Host SHA256: `a4d7d1b14e440232d384d430420817c8a38ba3677916230404c9b2df7b71fab7`. Frontend/model hashes did not change; final compiled real-input verification remains pending.
- Windows CRLF line endings in frozen Editor/runtime JSON are preserved intentionally. Whitespace review uses Git's `cr-at-eol` interpretation so hashes are not altered merely to normalize newline bytes.

## Frozen final directory candidate

- Final build `dist/CuteMaple-Live2D-Directory-20260909-195812` was compiled from clean commit `871e182a620e81e31453990885e7fc4a229f01da`. Its build gate passed 598 Python tests in 308.80 seconds. Main executable SHA256: `98c044471c59de3bb2d8ba0e08ca82e4af5073fc2d9459cefb0900ead405ecd8`; bundled cleaner SHA256: `ba5ab816555c1de3fb29171d5cdde126ed67ab6663a3908d7431fbb804d59b2d`. This replaces the obsolete helper for acceptance, without changing the preserved v4 distribution.
- Continuous native visual review accepted the final model on black, white and checkerboard backgrounds, bound in `artifacts/native-v5-visual-05/SOURCE-MODEL-QA.json`. Five successful continuous recordings cover locomotion, four cleanup postures, both wall transfers and interactions. Brief inherited painted head-view blending remains documented; this review does not substitute for real desktop input or cleanup results.
- Final-package candidate startup and packaged smoke checks passed in `artifacts/v5-final-acceptance-20260909-195812`. The candidate startup observed all four default Defender protection flags enabled and no relevant detections during its recorded interval. Packaged application, native runtime and non-privileged helper diagnostic exited 0.
- Twenty ordinary desktop startup/exit runs completed. The same unchanged bundle began its one-hour full scenario at `2026-09-09T12:17:56.317229+00:00`; it is still running at this checkpoint. Native exception text `0x8001010d` was observed during initialization in some logs and requires an explicit review of later activity, exit status and Windows events. Neither the long run nor the native exception review is marked complete here.
- Three real cleanups, a real rapid repeat click, ToDesk typing, audible endpoint switching and physical paused drag checks remain separate acceptance requirements. The prior canceled authorization has not been retried. The directory remains `UNVERIFIED`; no v5 release ZIP has been created.

## User desktop review rejected candidate 195812

- The user physically dragged the new desktop candidate and rejected the climb pose, double-click happy shaking, falling and landing. Earlier contact measurements and the native visual review are historical evidence; they did not establish adequate natural movement. Candidate 195812 is now marked requiresRecompile and must not be finalized. Its ongoing stability run remains diagnostic evidence for that unchanged bundle only.
- Motion inspection found happy repeats four bounce peaks, unsupported falling selects the rope-holding arm mapping, and climbing translates whole legs without phase-dependent knee/ankle articulation. These require new choreography and a bounded rig revision with another native export.
- Controlled real Windows process tests reproduced a status JSON read/atomic-replace race (WinError 5), including at the ordinary 400 ms observer cadence. The cleanup fix and all controlled fault/recovery scenarios must pass before building the next helper. No new UAC approval or real cleanup is inferred from the user interaction replies.

- Additional user-required release corrections: climbing skirt folds must follow knees and weight transfer; waking must not shake; falling arms must stay connected and return naturally; interaction controls must have readable text/background contrast; wind-stroke particles must be removed; cleanup dust must originate near the actual fan, never the forehead.
- Source cleanup JSON IO now uses a bounded Windows per-file shared/exclusive lock around reading and replacement. The original 20 ms controlled lifecycle run passed all four faults followed by four same-profile recoveries (41 owned process identities exited; no active lease). Evidence: artifacts/v5-cleanup-controlled-final/summary.json. This does not replace three real compiled-helper cleanups.
- Wake review found that sleep_exit ground tracking continued to read the hidden sleeping skirt. Native05 changes from that hidden anchor to the standing foot by about .1703 canvas units at idle, causing a roughly 37.5-pixel window jump at default size. The fixed-canvas recording could not reveal this desktop placement bug. A contact-plane correction is still being developed and must be checked on the desktop.

- The superseded 195812 bundle completed 20 ordinary starts and a 3601-second desktop run, main exit 0, audio child exited, bundle hashes unchanged; all four Defender protection flags remained enabled and the recorded detection inventory was empty. Its overall release result remains PENDING because external/manual gates and exception review are separate, and the user rejected the animations. No later model/source may reuse this run as final-package evidence.

## Motion refinement after the desktop rejection

- The new source revision is staged separately at `assets/authoring/revisions/v5-motion-refined-20260909`. Its planned native inventory has 143 drawables and 72 parameters. Native MOC/parameter/drawable identities are checked against the complete authored lists; the prior native model and authoring archives remain preserved.
- Happy is now one smile/head tilt and settle, without repeated body bouncing. The desktop window does not chase animated foot bounds during this reaction. Sleep enter/exit use a continuous painted-sole ground plane shared with idle, and standing shoes retain their original proportions while the body sits/rises. The old hidden-skirt ground anchor is removed from these states.
- Ground pickup, falling and landing use the original sleeve and hand artwork throughout, with shared wrist deformation and a gentle outward pose. Landing returns those same sleeves to rest rather than fading between unrelated sleeve paintings. These changes require the new native export; source renders alone are not final acceptance.
- Refined climbing uses a 1.6-second knee/ankle and weight-transfer cycle with .12 canvas rise and localized skirt folds. A single pair of handoff legs moves to the swing and matches the receiving geometry before exchange. A token-bound native cycle-endpoint handshake is being verified so the host can enter the transfer from its authored support pose.
- Interaction buttons/dropdowns now explicitly set readable foreground/background colors, including hover and disabled states. Wind-stroke particles were removed. Dust requires actual visible fan/hand geometry and originates along the fan surface; missing fan geometry no longer falls back to the forehead.
- A compact supplementary official-Core verifier now checks painted sleep contacts, handoff material/topology/pose matching, happy motion and free sleeve contacts. Its numeric results remain separate from production playback, real desktop placement and visual acceptance. The new final directory still requires all real cleanup, ToDesk/audio and stability gates.

## Refined authoring export checkpoint (22:39 local)

- The refined project and its 31-scene CAN3 have been built: 143 meshes, 72 parameters and 386 deformers. The packed Editor project is `F:\cutemaple\Maple-v5-Export-MotionRefined\Maple.cmo3`, SHA256 `220b399578517694568cd08141fc3ab2db5cac7397e79034188b103cf4aa0a0d`. Its single atlas contains every mesh and preserves source pixels. An exact four-file pre-export snapshot and per-file hashes are preserved at `assets/authoring/archives/v5-motion-refined-pre-export-20260909`.
- The combined authoring run passed 75 of 76 checks; the remaining assertion compared `1.0000000000000002` to `1`. It now uses a `1e-12` floating-point tolerance, and all 17 tests in that module passed on rerun. No geometry limit was relaxed. Reports: `artifacts/v5-motion-integrated-source-final.xml` and `artifacts/v5-motion-integrated-source-authoring-retest.xml`.
- Native climbing endpoint handling is implemented for normal wall-to-top transfer and cleanup entry, including first/late cycles and stalled-frame recovery. The host/frontend freeze passed 110 Python and 40 JavaScript checks. The performance-details displacement-pause branch still uses an ordinary cleanup fade and requires actual visual review; it is not claimed to have endpoint alignment.
- Finalization now requires a supplemental refined-motion report for this revision. It reopens the raw Core samples, all 31 current motion inputs, complete mesh identities, original paint and verified UV geometry, and recomputes the four numerical checks. These checks cannot approve appearance or replace real production/desktop playback.
- Cubism activation works, but observed Open/menu input again failed to reach this Editor instance. The user has been asked to export the new project with hidden parts and meshes. No new `Maple.moc3` exists in the refined export directory at this checkpoint; neither the new native model nor final compiled package has passed acceptance. The current canonical runtime is still the preserved previous export.
- An independent review found a numeric false positive when a visible hand coincided with a fully hidden cuff. Free-motion checks now record both opacities, require visible contacts on both sides, and require both original hand/cuff pairs and hidden donor contact artwork at free-motion endpoints. Six new negative cases and the existing report contract tests passed together (46 tests; `artifacts/v5-refined-release-contract-visible-cuffs.xml`). This changes the verifier only; the frozen Editor project and runtime motion bytes are unchanged.
- The refined native recorder now waits for the actual climbing endpoint before wall-to-top or wall-cleanup entry, and for actual finished/cycle events before ending landing, sleep or cleaning clips. New `drop-land-idle` and `sleep-exit-idle` scenarios retain the complete recovery and subsequent idle. Eleven scheduling checks and CLI loading passed (`artifacts/refined-preview-scheduler-tests.xml`); no new-model recording has run yet. Its fixed-canvas scope remains separate from PetWindow placement.

## Native export 06 and climbing drape correction

- The user's 22:50 native export is valid. All 143 native meshes match the packed UVs within 0.0007241 atlas pixels (`artifacts/native-v5-refined-export-06/native-uv.json`); exact authored atlas pixels were installed into the isolated `Maple` runtime there. Existing Editor output and the canonical prior runtime remain preserved.
- The actual production renderer completed 31 clips and 68 captures, but 40 left active hand/cuff measurements failed the unchanged .012 canvas threshold (`artifacts/native-v5-refined-runtime-06/report.json`). The independent 594-frame Core run likewise failed: sleep and happy passed, while 18 left cuff measurements and four handoff entries failed. The largest handoff gap was .03418 and cuff gap .01572 (`artifacts/native-v5-refined-core-06/summary.json`). These are failures, not final acceptance.
- Native/source comparison established two distinct causes. Handoff mesh positions had baked the approximate offline warp, while the real original leg used Cubism's different nonlinear interpolation. The native handoff reproduced its baked vertices, but those differed from the live original leg. The left cuff already had .0065–.0079 native error in both old 05 and new 06 with refinement disabled; the new deformation amplified it. Independent Core comparisons show that disabling refinement reproduces the old native baseline within 2.7e-8, so this was not a changed default or damaged export.
- Actual continuous climbing (`artifacts/native-v5-refined-visual-06/climb-right.gif`) confirmed the user's drape concern: an old skirt-shortening layer and new lower-skirt lift combined into an implausible raised hem. Source correction disables the old fold only in the refined pose and creates a knee-local front fold with a hanging rear hem. The complete 21-phase left/right and 19-frame transfers were reviewed in `artifacts/v5-climb-refinement/iteration-09-final-drape`; upper-body appearance there depends on the arm work in progress.
- New `leg_l_climb_refined` and `leg_r_climb_refined` reuse the same original images/UVs and store their phase shapes directly, with an affine mirror. The handoff references that actual leg family. Explicit near/far metadata selects the family in the Core verifier and release recheck. Near-zero family selection on the existing Refine/Handoff axes prevents old or intermediate climb legs from reappearing during pickup. Thirty-eight climb checks passed; the highest old-leg opacity over nine real motion blends was .000296, below .001 (`artifacts/v5-climb-refinement-final-freeze.json`).
- Shared native hand/cuff registration is being completed with exact material contact vertices; source-only coincidence between two independent nonlinear chains is no longer sufficient. The ground sleeve/hand artwork remains the same through drop and landing. A failed fixed-basis candidate enlarged palms/fans and was rejected; shape normalization and whole-pose review are required before the next native export.
- Short software recordings captured fewer than 100 frames under concurrent authoring load. Those failures and their actual PNG frames remain preserved. Refined recording now continues the final resting animation, within its existing timeout, until at least 100 real frames are captured; it never duplicates frames or shortens preceding motions. The subsequent sleep/wake/idle clip completed 100 frames over 14.24 seconds, retaining the full native motion completion and actual additional resting tail. This is fixed-canvas evidence only, not desktop window placement acceptance.
- New source work is isolated in `assets/authoring/revisions/v5-climb-drape-20260909`; the 06 CMO/MOC/UV evidence is not overwritten. The freshly opened Editor window also failed to respond to a directly observed File-menu click, so native export may still require the user's help after the complete new project is built and reviewed.

## DrapeFixed source checkpoint (23:53 local)

- The complete new source project has 145 meshes, 72 parameters and 328 deformers. Its packed project is `F:\cutemaple\Maple-v5-Export-DrapeFixed\Maple.cmo3`, SHA256 `a7124dd51bf08d73c4a2071ba2a30b8d1b3ad3421e140dbf6ddc22d3bf81199c`. The relinked CAN3 contains all 31 scenes. One rebuilt atlas covers all 145 drawables without altering the 141 original source PNGs; the four packed authoring files are preserved before native export in `assets/authoring/archives/v5-climb-drape-pre-export-20260909` with per-file hashes.
- Active palms and fans now share the exact lower-sleeve native parent hierarchy through a contact-centered affine registration. Explicit UV contact vertices are added by local triangle subdivision; original vertices, UVs, PNGs and the ground hand/sleeve chains remain preserved. Pose-dependent normalization removes the first candidate's oversized palms and fans. Thirty-two whole-pose transfer/cleaning source frames were reviewed after normalization. A separate read-only writer review found no serialization contract that would cancel this registration, but native Editor/Core confirmation remains required.
- Focused source regression passed 38 climbing checks, 27 shared-cuff checks, and 26 integrated authoring/sleep checks. These cover the actual failed 06 contact parameters, visible leg-family transitions, handoff endpoints, and original ground artwork. Reports are `artifacts/v5-climb-refinement-final-tests.xml`, `artifacts/v5-native-cuff-shared-chain/tests-normalized-02.xml`, and `artifacts/v5-drape-integrated-authoring.xml`. These are source evidence, not a passed native or desktop acceptance result.
- The user has been asked to export this new DrapeFixed project with hidden parts and meshes because directly observed Editor input still failed. The previous native06 export is valid but visually and numerically rejected; it is not the new model. No DrapeFixed MOC3 or final compiled bundle existed at this checkpoint.
- Independent release-gate review found that the existing raw samples already cover mid-cycle climbing, but their leg opacities were not checked outside the handoff group. The verifier now also requires exactly the appropriate visible leg pair at those pure-climb samples (both directions, phases .35 and .7); true fades retain their separate checks. All 166 related tests passed, including eight old/new-contract third-leg rejection cases (`artifacts/refined-midcycle-leg-gate-tests.xml`). Native sampling and numeric thresholds are unchanged.
- The first whole-suite attempt could not create pytest's shared system temporary directory under the ordinary desktop process identity (`WinError 5`). That ineffective, identified pytest child was stopped; its source-stable failure summary is retained as `artifacts/v5-drape-full-suite-01.summary.json`. A new whole-suite run uses an explicitly checked, new workspace temporary directory and disables the shared pytest cache, matching the existing build script's isolation. Windows protection and existing directory permissions were not altered.

## Source regression interruption and recording coverage (2026-09-10)

- The second whole-suite process accumulated roughly 17 GB while two existing Cubism processes held roughly 16.7 GB and 6.6 GB. Windows System event 2004 at 00:10:12 local confirms virtual-memory exhaustion, identifying this exact pytest PID 29668. The test connection ended without a final XML/summary, so the run is not a pass and its displayed errors cannot be dismissed without rerunning the affected tests. Exact event evidence is retained in `artifacts/v5-source-regression-memory-interruption-20260910.json`. A process-isolated regression runner is being added so heavy authoring fixtures release all memory between groups. Existing Editor sessions and system memory/security settings remain untouched.
- Recording now also checks coverage separately for each native play token/name: at least three distinct evaluated, visible native frames and a native-clock span covering 60% of that step's expected presentation time. Existing native completion/endpoint gates and the 100-frame minimum remain required. Late QWebChannel responses are attributed to the native token, rather than callback receipt time; later idle captures cannot fill a missing falling or waking segment. All 24 recorder tests passed (`artifacts/v5-preview-coverage-final-20260910.xml`).
- Retrospective checks of the immutable native06 sleep/wake and climbing recordings satisfy the new per-step capture gate; the waking segment has eight valid distinct native frames, and the climb-to-top segment has nineteen. The supplemental result is `artifacts/v5-preview-coverage-real06-20260910.json`. It establishes recording coverage only: the old native06 climbing model remains rejected, and no DrapeFixed native model or final-package acceptance is inferred.

## Native DrapeFixed export 07 (2026-09-10, 08:43 local)

- Observed Cubism input recovered. The assistant opened the saved DrapeFixed CMO in a new tab and completed the native MOC export, including hidden parts and meshes, while preserving the existing unsaved Editor work. The manual export request was withdrawn. Native MOC SHA256 is `380b4769623a75c18859f275f7a9531e50b9afc2c44a237f0ee490f111c6f874`; the packed CMO hash remains unchanged.
- All 145 native drawables and atlas mappings passed, with maximum UV error .0007241 atlas pixels. The original authored atlas was restored only into the isolated runtime after this complete UV proof. Exact evidence is in `artifacts/native-v5-drape-export-07`; the canonical prior runtime remains unchanged.
- The independent 594-frame official Core audit passed sleep ground contact, happy single-cycle/face proportions, and all handoff geometry/family checks. Worst handoff endpoint difference is .00003064 canvas. It still **failed** 36 active hand/cuff contacts, with worst separation .022387 canvas. The production renderer independently completed 31 clips/68 captures but also failed cuff contacts, including the swing. Source coincidence through the extra hand-registration warp is therefore insufficient. Reports remain preserved in `artifacts/native-v5-drape-core-07` and `artifacts/native-v5-drape-runtime-07`; no threshold was relaxed.
- Actual right and left climbing recordings completed 197 and 205 real frames over approximately 20 seconds each, including native endpoint handshake, transfer and swing. Both passed recording coverage, which is not visual approval. Full-body review shows the rear skirt hanging while a front fold follows the raised leg; the old whole-hem shortening is absent. The active wrist defect still disqualifies the full animation. Fixed-canvas recordings do not establish screen-space grip stability or actual PetWindow displacement.
- The new process-isolated full source run passed all six heavy groups, but the remaining group produced 8 failures and 20 errors (790 passed, one skipped overall). A bounded long/short-path comparison reproduced two cleanup-fixture failures only with the long test path; temporary-path shortening is being fixed in the runner. Four Qt setup failures are being investigated separately. The failed report is retained at `artifacts/v5-drape-isolated-regression-20260910-01`; it is not a passing regression or final delivery.
- The runner now uses short `t01` through `t07` temporary paths and rejects unsafe Windows path lengths before launching pytest. Runner/cleanup verification passed 59 tests; the four failed Qt setup cases separately passed with unchanged sources and no concurrent native diagnostic. A second Windows memory-exhaustion event at 08:47:45 identified the two existing Editor processes and a native QA Python process (`artifacts/v5-native-regression-memory-event-20260910.json`). Further heavy authoring, native and Qt runs are serialized. No application was killed and no system settings were changed for this correction. A fresh complete serial source run remains required.
- Independent full-frame review also rejected native07's lower-body clarity: shoes cluster during recovery (right frames 22/24/26), and the front skirt remains a similar wedge while the legs move at its edge. The next revision must improve the free-foot departure arc and knee-local folds while preserving planted-foot travel and the hanging rear hem. `artifacts/native-v5-drape-visual-07/independent-review.md` records exact frames and separates these visual findings from fixed-canvas limitations. It also identifies about 5.6 pixels of inferred support-anchor drift under the current host travel formula; this must be rechecked after the wrist correction and in an actual PetWindow.
- The new cuff source correction centers the shared wrist on an actual control point of its child lattice, preserving source UVs and the existing mesh parameter allocation. Eighteen focused full-rig checks passed, including all pose/fan/arm-mode contact combinations and a small independent nonlinear-parent resampling case. Source hashes and allocation proof are in `artifacts/native-v5-drape-core-07/cuff-fix-freeze.json`. This is still source evidence awaiting the next native export, not an accepted fix to native07.
- A second concrete climbing mismatch was found using identical-parameter native frames: the host aligns the near hand's mesh edge to the screen, but both planted leg meshes stop short by .036414 canvas on the right and .027713 on the left. Mapping the original opaque shoe contours through the verified native UVs gives actual painted gaps of 14.92 and 11.58 pixels at a 384-pixel pet height. This is native geometry plus the exact host X formula, not an observed moving window. Raw hashes, matching frame IDs and per-shoe results are preserved in `artifacts/native-v5-drape-core-07/foot-wall-inference.json`. The next source candidate shifts the whole lower-leg skeleton outward .032 canvas without changing bone lengths, vertical support travel or timing; its real painted contact must be recalibrated against the newly exported hand geometry.

## Native CuffFixed export 08 (2026-09-10, 09:23 local)

- The assistant exported the separately packed CuffFixed project in Cubism, including hidden parts and meshes. Existing Editor tabs were preserved. Source revision is `assets/authoring/revisions/v5-cuff-lattice-20260910`; the packed source archive and hashes are under `assets/authoring/archives/v5-cuff-lattice-pre-export-20260910`. Packed CMO SHA256 is `5af8f1c82b97c61a428f5e86e08a3d5b6b30be76563f845b1e0b81e36be8474d`; native MOC SHA256 is `57d97329df00cb60040fac891b7c4a13a428700a8b05539136413f5c53561317`. The new CAN3 is structurally generated, with Editor playability still pending.
- All 145 drawables and native UV mappings passed unchanged thresholds. The isolated runtime retains original authored atlas pixels after that proof. Its 594-frame official Core audit passes all four existing gates: sleep contact, climbing handoff, happy single-cycle/face proportions, and hand/sleeve contacts. Maximum active cuff error is `5.985563147187384e-7` canvas; the maximum across original resting cuffs is `.0001405106754759063`. The production Qt renderer independently completed 31 motions, 68 captures and the interaction/geometry probes with `passed: true`, no errors. Evidence: `artifacts/native-v5-cuff-export-08`, `artifacts/native-v5-cuff-core-08`, and `artifacts/native-v5-cuff-runtime-08`.
- The additional 202-frame full climbing-cycle contact audit is complete and input-bound, but exposes remaining issues outside those four existing gates. At a 384-pixel pet height, the right painted planted shoes remain 3.83–4.68 pixels short of the host's actual near-palm edge; left gaps range from -.066 to .711 pixels. Nominal-host planted wrist drift reaches 3.46 pixels for the left far hand across the cycle wrap. `artifacts/native-v5-cuff-contact-08/measurement-summary.json` retains the raw input hashes and worst frames. Measurement completion is not contact or visual acceptance.
- Actual native right and left climbing recordings completed 198 and 208 frames, including transfer to swing. Their event/coverage gates pass. Full-frame review still rejects the skirt/leg relationship: the skirt retains too much of its standing outline while the bent legs appear at the front edge. The local fold's coordinate space relative to the body/profile parent is being checked before further changes. No native08 model has been promoted to the canonical runtime or packaged for final delivery.
- Native08 drop/landing/idle, sleep/wake/idle and interaction recordings also completed their native-event and coverage gates (118, 103 and 144 frames). Reviewed samples no longer show the old free-hand detachment or repeated happy/wake bouncing. These fixed-canvas previews still do not prove actual desktop fall travel or hardware typing/audio input. Recordings and unmodified-frame sheets are under `artifacts/native-v5-cuff-visual-08`.

## Painted climbing contacts and cloth material correction (in progress)

- Local commit `365f4d2` saves the native08 source correction and isolated regression runner before the next geometry revision; native08 remains a rejected climbing candidate. No GitHub push was made.
- The skirt's nominal .10–.14 canvas fold was mostly outside painted pixels: right profile material stops at x=.645 while the old crest was around .688–.699 before the outer body shift. Actual orange/front-panel lift was only about 1–3 pixels at 384 pixels. Per-view material registration and the actual painted panel bounds now place the fold over the hip/thigh region, with the original rear train and source UVs retained. Lower-body source previews show a meaningful gather; complete source and native validation remain pending.
- The production frontend can bind an `atlasUv` contact to its actual native triangle once, then carry that same material point through each evaluated frame. It rejects missing/outside topology and uninstalled `sourceUv` descriptors. This avoids switching among extreme padded vertices. The installer must supply the audited native atlas orientation (Core V is the inverse of the PNG V). All 43 frontend tests pass, including a real installed Core/model integration case; TypeScript compilation and offline build pass.
- Climb-to-top now selects a material `nearGrip` when provided and pins that anchor key throughout transfer. A missing material report does not silently switch to a padded edge. Legacy models retain their initial legacy key. Nineteen focused host tests pass, including both directions with changing padding and missing material evidence (`artifacts/v5-material-anchor-host-final.xml`). The new metadata must provide the same material descriptor in both climb and handoff states before it can be used.
- Offline rasterization previously let later transparent triangles erase earlier painted pixels within the same drawable. The diagnostic renderer now composites fragments in order, batching disjoint triangles; shared edges are owned once. Eleven small synthetic tests cover overlap, winding, clipping, opacity and draw order. Native rendering and original PNGs are unchanged; this removes misleading source-preview holes only.
- The first combined painted-cloth source build produced 145 parts, 72 parameters and 332 deformers, with 42 complete-cycle full-body source frames. Actual painted-foot tests pass both directions with the original `.0001` canvas tolerance; the sole measurement excludes transparent padding. Source CMO `a4a24347d48d583259a32ee8da6bbf7fdd05f20f2c0b822082192ea2893674bf` and its packed project are preserved as diagnostic revision 09, without a native MOC export.
- Independent review caught a new transfer defect before native export: the common arm contact correction remained active after the far hand acquired the rope at .32 seconds. The correction now uses the existing signed PoseClimb driver for per-arm transfer timing, keeping three axes; the far correction reaches zero at rope acquisition, while the near wall hand retains its existing gated correction. Eleven focused source tests pass, including .32/.55/.85-second rope holds and unchanged pure climbing. The separate `v5-painted-climb-rope-20260910` revision is being rebuilt for native export; diagnostic09 is not substituted for it.
- Roof proximity now prefers the actual material nearGrip as well, so legacy padded aliases cannot trigger an early top transition. Twenty-one focused host checks pass (`artifacts/v5-material-anchor-host-roof.xml`). The process-isolated suite also fingerprints the calibration JSON as a code input; ten runner checks pass, including detection of a changed calibration file. Full final-source regression is still pending.

## PaintedClimb RopeFixed source checkpoint (2026-09-10, 10:16 local)

- Revision 09b is built separately at `assets/authoring/revisions/v5-painted-climb-rope-20260910`, with 145 parts, 72 parameters and 332 deformers. Source CMO SHA256 is `d36b19c64e0c2e0e47a28ee9be1d7c50e18ce7742bfc11aa33906d90d43c01bd`. Ten source transfer frames cover both directions at 0/.32/.55/.85/1.8 seconds; these are diagnostic renders, not native export acceptance (`artifacts/climb-transfer-ir-review-09b`).
- The packed project is `F:\cutemaple\Maple-v5-Export-PaintedClimb-RopeFixed\Maple.cmo3`, SHA256 `127928da21fca8d410ff0e786f971f7c9b10e114b048aa440c054843e488e60b`. Its relinked 31-scene CAN3 SHA256 is `0d2f598d9b12c47bfc27f884ad94c556d5cf47680db3d7e8cdf1b9e19206a664`. Exact CMO/CAN/atlas/audit copies and hashes are preserved under `assets/authoring/archives/v5-painted-climb-rope-pre-export-20260910`; original source PNG copies are separately hash-verified.
- Native export remains pending. The Editor process reports responding, but freshly observed click and shortcut attempts did not open its File menu or Open dialog. The user has been asked to confirm the desktop is unlocked and the menu responds. Existing Editor tabs were preserved; no process was terminated, no ToDesk disconnect was requested, and no security or memory settings were changed.
- The next native review includes actual painted palm/shoe contacts across both climb cycles and actual visible fingers against the rope during transfer. Wrist-center distance to the rope is diagnostic only: a wrist is not required to coincide with a grasped rope. Source 09b is not yet installed as the canonical runtime or packaged.
- The complete installer/material-anchor modules now pass 44 tests, and the complete desktop Live2D integration module passes 105 tests, all with no failures, errors or skips. They ran in separate ordinary-sandbox processes with short temporary paths; relevant source/config/fixture hashes stayed unchanged (`artifacts/fv01`, `artifacts/fv02`). No full final-source or native09b visual pass is implied.
- Restarting the computer-tool JavaScript session recovered screenshot capture, but a fresh Open shortcut and File-menu click still did not enter Cubism. Native09b export is still absent at this checkpoint. Packaging must retain the exact post-install metadata and `editor-export.json` receipt used by native QA, since both belong to the runtime resource snapshot.

## Complete source regression checkpoint (2026-09-10)

- Local commit `5e87b30` preserves the 09/09b source revisions, packed pre-export archive and runtime code changes. All seven isolated regression groups then passed: 866 tests passed, one skipped, zero failures/errors, from 10:25:54 to 10:39:12 local. The unchanged Python/config/fixture aggregate is `a1c665459620e20fc7d246259ab10e9b8f7d9ccaae1bcf3aaa1947ee05650bcb`. Report: `artifacts/r09b01/summary.json`, SHA256 `f453d46cdfcebf056fc7d3bcfe918ebe6d05f2fafca924ba2579be758f655067`; each group retains its original JUnit, stdout/stderr and source check.
- The sole skip is the symlink-race fixture because the ordinary host lacks symlink-creation privilege (WinError 1314). No privilege or operating-system setting was changed to run it. This source result does not replace real compiled-helper cleanup or final desktop acceptance.
- The suite used the preserved canonical05 runtime: MOC SHA256 `5cd77005ea7c5ac9ef02a661d02a30334003d69264e30c3dbaadcfc5cd237ed1`. Its 38-file snapshot was captured during the run and rechecked unchanged afterward (`artifacts/r09b01-canonical-runtime-scope.json`). Tests that consume the replaced runtime, including installer fixtures, need fresh verification after 09b promotion; this report is not native09b certification.
- At 10:48 local, `F:\cutemaple\Maple-v5-Export-PaintedClimb-RopeFixed\Maple.moc3` is still absent. The user-facing desktop unlock/menu check remains unanswered. No new model has been promoted and no final ZIP has been produced.

## Native PaintedClimb RopeFixed export 09b (2026-09-10, 11:18 local)

- After the user confirmed an unlocked desktop and responsive File menu, the previous Editor process/window was absent. The assistant opened the installed Cubism Editor, loaded the exact packed09b CMO and completed native export with hidden parts and meshes included. No existing Editor process was closed. The new MOC SHA256 is `c497bcb9457ec232104c2b904023e799e75dbf5bf5a2c6977091066ce9868a26`. All nine original export/input files are copied and hash-verified at `assets/authoring/archives/v5-painted-climb-rope-native-20260910`.
- Official Core geometry checks passed for all 145 drawables. All 145 native UV mappings match the packed atlas within `.0007240773440362347` atlas pixels. The isolated runtime is `artifacts/native-v5-painted-export-09b/Maple`, with 31 motion groups and original authored atlas pixels restored only after the full UV proof.
- The installer converted 28 authored source-material anchors into verified native atlas coordinates. Installed metadata SHA256 is `b7047470cfcfb3f4d365c0b5dc9356b414966391f400a514b6682961d1819f1d`; source metadata remains unchanged at SHA256 `c16fe91b6a94ada4801b8d3ec1f4d69845ed8e683ffd6411dc72969f562a91e2`. The receipt records both identities. Detailed native contact checks and production visual playback are now pending; canonical05 is still preserved and no final package has been built.

## Native09b visual review and follow-up corrections (2026-09-10)

- Native09b passes the existing 594-frame Core checks, the 202-frame painted-contact measurement, the six first-rope samples, and the production Qt 31-motion/68-capture checks. At 384 pixels the planted shoe/wall gaps are below .552 pixels and planted palm world drift is below .205 pixels. These numeric results do not override visual rejection. Evidence is under `artifacts/native-v5-painted-{core,contact,rope,runtime}-09b`.
- Actual native recordings cover left/right climbing and transfer, drop/landing/idle, sleep/wake/idle, interactions and cleanup. The first cleanup recording failed its final .6-second resting-tail capture span; a fresh unchanged retry passed with 208 frames. Original failed evidence remains. The successful recordings and unmodified-frame review sheets are under `artifacts/native-v5-painted-visual-09b`.
- Climbing cloth now gathers over the raised knee and retains the hanging rear train; reviewed drop/wake/happy samples no longer show the prior detached hands or repeated bouncing. However, left transfer frames 0086 and 0090 show doubled painted facial features and buns while two head views exchange. Their actual material locations were not aligned by the coarse head guide. Source correction retains the existing continuous 65-square head warp and original view opacity keys, with measured material controls and native-style lattice/mesh sampling constraints; actual new native playback remains required.
- A second defect is precisely identified in native frames 0084/0085: `leg_r_climb_handoff` paints the thin white point below the skirt. A reflected left shoe was blended through a world-space residual while the ankle rotated. The correction retains reflection in the rotating material frame, keeps toe-axis length, and exchanges material facing only during the tucked interval; original climb/swing endpoint vertices remain authoritative. Five small geometric regressions pass; the corrected actual silhouette/occlusion is pending.
- Cleanup's original round fan becomes a thin strip in ground/top native poses. The fan now has its own 17-square registration lattice under the exact same sleeve parent and wrist center; the existing three-square palm registration is unchanged. This targets native parent-lattice sampling across the much larger fan surface. A synthetic nonlinear-parent wrist/opening test passes; actual native fan shape and shaft contact still need verification.
- The assistant opened the 31-scene CAN3 in Cubism, played idle/happy, and saved an independent native copy at `artifacts/native-v5-painted-can-09b/Maple-native-saved.can3` (SHA256 `476b3e68cc589349a683b39e8538b0d977ddacc6755d17433ec60c13a40b1362`). All 2,232 parameter lanes and 100 fps scene timing remain, but native loading recalculates controls on visited scenes because the serializer used `SMOOTH`. Maximum observed control-value change is .11781. Explicit authored curves now use `BEZIER`; new native open/save comparison remains pending. Original runtime motion3 and packed CMO files were not altered.
- Follow-up source is staged separately at `assets/authoring/revisions/v5-head-shoe-fan-20260910`. Native09b is not promoted or packaged; v4 remains available. The assistant closed only the Editor instance launched for this verification, discarding its temporary model playback state after saving the independent CAN3 copy.
