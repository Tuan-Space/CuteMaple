# 当前可编辑模型

当前工程和完整配套素材都位于本仓库。请参阅 [开发与编辑指南](../../docs/DEVELOPMENT.md)。

`model` 是当前原生模型与动画工程，`source` 是当前制作输入；两者不是历史发行版。生成曲线和 Cubism 原生保存的曲线都保留，可用于比对。打开动画前可在根目录运行 `open-editor.ps1 -Animation`，关联会自动指向当前下载位置。
