# 当前 Cubism 工程

`Maple.cmo3` 与运行模型对应，159 个网格、80 个参数，工程贴图已内嵌。
`Maple-native-saved.can3` 是 Cubism 原生保存的动画工程，`Maple.can3` 是对应生成曲线。

使用根目录的 `open-editor.ps1 -Animation` 按当前下载路径关联模型，不需要作者电脑的目录。
原生导出参考数据和图集核验数据保留用于后续编辑校准；实际播放器文件以 `assets/live2d/Maple` 为准。
